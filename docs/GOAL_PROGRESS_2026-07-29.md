# OntologyOS Goal Progress - 2026-07-29

The long-term OntologyOS Industrial Intelligence goal remains active. This increment establishes production boundaries and verified compatibility; it does not claim completion of the roadmap's external-evaluator, availability, or largest-scale gates.

Latest verification refresh: 2026-08-11.

## Delivered

- Normalized, project-scoped ontology property and resource definitions compiled from current object, link, action, and profile resources.
- Published Workshop modules, visual artifacts, and delivered Pipeline Builder graphs now bind to the exact active ontology revision, checksum, object/action targets, referenced properties, and structured source paths. Consumer versions are immutable and retained for audit; additive revisions report `COMPATIBLE_STALE`, removed targets or fields report `BROKEN`, publish/rollback return downstream health immediately, and property-aware impact analysis avoids unrelated consumers. Production publication fails closed on missing or drifted contracts, and project snapshots preserve binding history.
- React Ontology Manager now renders downstream contract health in the object overview, Contracts section, and release studio; semantic review names affected consumer versions, breaking publication requires an explicit human acknowledgement, and publish/rollback move the next authoring base to the actual production revision. The backend recomputes impact at publication and accepts only the exact acknowledged immutable binding-ID set, rejecting missing and stale acknowledgements to close validation-to-publish races. Browser acceptance covers additive publication, exact consumer binding, current health, breaking impact, and the disabled-until-acknowledged publish control.
- Append-only object change events with actor, source, transaction time, valid time, before/after state, changed fields, ontology revision, and evidence.
- Additive `/api/v1` compatibility surface with typed object filters, aggregation, cursor pagination, temporal/GIS reads, masking, and bounded graph expansion.
- Immutable local or S3-compatible dataset snapshots using Parquet/JSONL, Arrow materialization, and parameterized DuckDB queries.
- Compiled pipeline plans with graph-version checks, schema, field lineage, validation, deterministic hashes, and a pluggable executor boundary.
- Provider-neutral model gateway with deterministic local inference, explicitly enabled OpenAI-compatible providers, secret references, allowlists, budgets, idempotency, evidence, and action-proposal-only behavior.
- Versioned durable agent task APIs over existing queue, lease, retry, cancellation, approval, and event-stream semantics.
- Persistent Docker snapshot volumes, optional MinIO, and independently checksummed local snapshot backup/restore sidecars.
- Advanced ontology field controls for indexed/sensitive fields, display names, units, ranges, enums, and regex constraints.
- React route splitting reduced the main entry bundle from 635.4 KB to 227.0 KB; builder/canvas and workspace code load as independent chunks.
- SQL-native typed object queries with temporal/spatial/filter/aggregate pushdown and stable null-aware keyset pagination.
- Governed ontology property index plans with administrator application, persisted DDL/status/error evidence, SQLite expression indexes, and PostgreSQL JSONB expression indexes.
- Bounded batched graph expansion with explicit node/edge budgets, deterministic traversal, composite link indexes, query-plan evidence, and sensitive-property masking.
- Transactional audit/ops outbox capture, leased idempotent dispatch, durable event log, retry/dead-letter recovery, resumable SSE, and production worker integration.
- Project-scoped backup/restore now preserves pending outbox delivery intents and published event-log sequences; in-flight leases recover as retryable work and restored evidence is not enqueued twice.
- Optional Kafka/Redpanda-compatible publication with production transport security, stable event identities, durable per-destination receipts, independent retries/dead letters, replay, broker partition/offset evidence, worker capabilities, and snapshot recovery.
- Durable event-time stream processors with atomic stream arrival sequences, partition-local watermarks, late/invalid-data quarantine, tumbling aggregations, stable output IDs, producer backpressure, asynchronous workers, transactional rollback, readiness evidence, and complete project snapshot recovery.
- Durable artifact review with revision-anchored comments, target-scoped change proposals, independent production approval, server-locked non-overlapping rebasing, durable conflicts, audit/receipt evidence, snapshot recovery, and a human React review panel.
- Two-replica PostgreSQL collaboration fencing now refreshes locked ORM state, atomically records revisions/receipts/events, safely rebases target-disjoint edits, and gates 20-editor acknowledgement p95 below 250 ms.
- Typed object-query masking is batched per ontology type, governed PostgreSQL indexes use planner-compatible composite `BTREE_EXPRESSION_V3` plans with stable keyset tie-breakers, index activation refreshes expression statistics, and strict smoke/reference benchmarks validate physical plans and latency.
- A mixed ontology workload now runs concurrent typed API reads while transactional batches update JSONB state and append temporal events, then verifies rollback atomicity, post-write index plans, statistics refresh, and pool-disposal recovery.
- The React Command Center now completes triage, approval/rejection, idempotent governed action execution, and evidence-backed report export through visible controls. Durable UI state links the current approval to its action outbox event, and report completion is recorded only after an export tied to that action.
- Object Explorer and Operational Map now run as typed, lazy React workspaces instead of evaluator-critical legacy routes. Explorer provides structured ontology queries, live facets, risk scores, saved views, profiles, selection, and governed actions. Map provides real Leaflet/OpenStreetMap rendering, persistent ontology layers, keyboard-accessible feature selection, risk styling, MGRS encode/decode, radius buffers, and audited geofence evaluation.
- Object Explorer and Map pass unchanged Playwright overflow and axe checks at 375, 768, 1280, and 1600 pixels; focused seeded workflows prove object search/profile and GIS feature-selection/MGRS behavior.
- ModelOps now runs as a typed, lazy React workspace spanning objectives, deterministic training, automatic/manual evaluation gates, releases, deployments, drift and quality monitoring, structured inference, and prediction-log evidence. A stateful Playwright test completes that lifecycle through visible controls, and the workspace passes unchanged axe and overflow checks at 375, 768, 1280, and 1600 pixels.
- Decision Intelligence now runs as a typed, lazy React workspace for risk evaluation, weighted explanations, append-only object timelines, deterministic entity review, before/after scenarios, and the governed agent runtime. A seeded browser workflow proves risk-to-scenario behavior, and the workspace passes unchanged axe and overflow checks at 375, 768, 1280, and 1600 pixels.
- Decision rules, scorecards, runs, temporal snapshots, entity-resolution jobs/candidates, and scenarios now carry enforced project ownership, migrate through `0034_decision_project_scope`, survive project snapshot export/restore, and reject cross-project object and mutation references. Agent, AIP Logic, automation, investigation, and Ops risk context now propagate the owning project through shared retrieval and scoring helpers.
- A new `/api/v1/industrial/workflows/asset-reliability/onboard` compiler turns any project-owned promoted dataset into a namespaced asset ontology, executable hydration pipeline, geometry/MGRS enrichment, append-only snapshots, explainable risk rule/scorecard, audit/Ops evidence, and linked Command Center outcome. The React form exposes field mappings without raw JSON, and the workflow is idempotent and tenant-tested.
- Project-owned industrial data now continues through `/triage`, `/workflow-state`, and `/report`: the deterministic agent cites decision, scorecard, and pipeline evidence; a project-namespaced ontology action requires approval; execution writes temporal snapshots and a transactional outbox event; and incident, investigation, evidence, and report records remain tenant-isolated. A visible-control Playwright acceptance completes this custom-data Connect-to-Report path and downloads the project report.
- Industrial onboarding now publishes the generated object-and-action contract through the living ontology lifecycle before hydration: manifest validation and compatibility classification, immutable production revision, normalized property/resource definitions, typed registry schema, publish-permission enforcement, checksum-idempotent reruns, and revision-linked object events. Unacknowledged breaking mappings roll back atomically instead of drifting the active schema.
- Project-owned industrial onboarding now crosses the scalable data-plane boundary: embedded inputs are snapshotted once, externally registered Parquet inputs work with empty metadata records, a deterministic DuckDB graph and immutable execution plan produce a content-addressed output snapshot, and ontology reconciliation processes bounded 1,000-row batches with field lineage and immutable contract-run evidence. Command Center proof links expose the source snapshot, plan, output snapshot, ontology contract, and governed decision chain.
- Large industrial onboarding now dispatches through the durable worker runtime with five-attempt leases, idempotent request receipts, deterministic plan/run/output identities, and independent 1,000-row ontology and decision checkpoints. Simulated worker loss after hydration row 1,000 and again after decision object 1,000 resumes without duplicate objects, pipeline runs, contract runs, decision runs, or snapshots. A 10,501-object contract evaluates every object, records complete risk-band counts, bounds detailed findings to 100, and exposes the aggregate run through refresh-safe workflow state and the React Command Center.
- Source-backed ontology materializations now have explicit generations and recoverable active/retired state. A later snapshot retires missing objects without deleting their properties or temporal evidence; current object/graph queries, risk evaluation, triage, and reports exclude retired state by default; returning source keys reactivate the same object identity. Retirement scans checkpoint every 1,000 objects and resume after acknowledged worker loss without duplicate retirement evidence.
- Every append-only ontology object change now creates a transactional `ontologyos.object_change` delivery intent in the same commit. Payloads carry object version, before/after state, changed fields, source, evidence, ontology revision, and materialization lifecycle. Rollback removes both temporal evidence and delivery intent; leased retry, replay, internal-log publication, and optional Kafka delivery retain one stable event identity. The hydration hot path skips redundant idempotency reads and uses O(1) pending-event lookup.
- Project-owned event-stream bindings now route durable platform events into ordinary processing streams through persisted topic, event-type, aggregate-type, and ontology object-type filters. Cursor advancement, deterministic stream records, exact receipts, and stream sequence allocation commit atomically; synchronous and leased `event.stream.route` workers recover without duplicates. PostgreSQL serializes competing routers with a binding lock, and project snapshots preserve binding cursors and receipts.
- Durable processors now support project-scoped two-stream inner interval joins with independent source/partition watermarks, normalized key-and-time state, either-side-first matching, many-to-many output, exact pair receipts, leased worker retry, and portable snapshot recovery. Pair output, input receipts, normalized state, run evidence, and target dataset mutation share one transaction. PostgreSQL serializes competing processors and retains indexed interval lookup plans.
- Stream correlation now supports `inner`, `left`, `right`, and `full` interval semantics. Outer records finalize only after the opposite side/key watermark strictly closes the tolerance interval; explicit monotonic watermark advancement handles idle keys, arbitrary accepted late data is rejected, and immutable outer receipts make retries, compaction, concurrency, and project snapshot restore duplicate-safe. A clean PostgreSQL rehearsal raced two finalizers over 50 unmatched records and produced exactly 50 outputs from one winner and zero from the serialized competitor; later would-be matches were quarantined behind the finalization fence.
- Operational Control now runs as a typed, lazy React workspace for events, alert rules, incidents, governed runbooks, reliability posture, and inbox acknowledgment. A stateful browser workflow proves event-to-alert-to-incident-to-runbook-to-notification behavior, and the workspace passes unchanged axe and overflow checks at 375, 768, 1280, and 1600 pixels.
- The complete current 196-case Playwright evaluator matrix passes after populated-state WCAG contrast fixes: 100 applicable checks passed and 96 viewport/workflow-specific checks were intentionally skipped by their declared conditions.
- Asset Reliability bootstrap, triage, summary, workflow, validation, and export routes now enforce project permissions; OIDC requests use the authenticated principal instead of accepting a spoofable actor label.
- Snapshot-native DuckDB pipeline plans register Parquet without row hydration, compile per-node branching SQL for joins and schema-aligned unions, execute through durable workers, write immutable multi-source lineage-bearing outputs, and replay the same delivery job without duplicate snapshots.
- Snapshot-native geofencing now keeps compiler size and parameter transport bounded for high-vertex operational boundaries: validated polygon rings become compact scalar edge tables, repeated telemetry coordinates are evaluated once, and hole semantics remain exact. The strict reference filters 10 million rows through 9,218 polygon positions with preview p95 1.46 seconds and delivery 1.52 seconds on the documented host.
- Real S3-compatible execution now has an impaired-network reference profile: the harness independently verifies a 40 ms downstream Toxiproxy toxic with 5 ms jitter, then runs 1 million rows across eight MinIO partitions. Four concurrent cold-cache workers finish in 541 ms while producing exactly eight downloads, 24 coordinated cache hits, and zero integrity failures; cold/warm public query and durable pipeline metrics remain separately reported.
- Snapshot-native DuckDB compute now runs inside independently deployable worker processes instead of the API process. A real PostgreSQL/MinIO/Toxiproxy rehearsal killed the first worker after it cached one of 32 partitions, requeued the expired lease as attempt two, rebuilt all 32 partitions in a replacement worker's private cache, and published exactly one fenced 10,000-row output snapshot. The bounded form is enforced in CI.
- The production image now passes the same boundary under real Keycloak OIDC and container isolation. A service account with only project execution permission claimed the job; the first worker container was killed with exit 137 after caching 14 of 32 impaired-network partitions, and a second container with a fresh tmpfs cache downloaded all 32, completed attempt two, and published one fenced 10,000-row snapshot. The release harness also verifies complete container, network, and volume cleanup.
- The stronger independent-runtime rehearsal loads the production image into two distinct Docker daemons with separate image stores, filesystems, and caches. The first daemon's worker exited 137 after caching 3 of 32 partitions; the second daemon rebuilt all 32, recovered attempt two, and published exactly one fenced 10,000-row snapshot. Both daemons run on one physical host, so this closes shared-daemon state as a confounder without claiming provider-network or host-loss validation.
- Durable jobs now support immutable, same-project dependency graphs without a schema-head change. Coordinators remain `BLOCKED` and unclaimable until every prerequisite succeeds, then move to `QUEUED` with explicit evidence; terminal dependency failure propagates deterministically with audit and observability records. Idempotent replay is resolved before dependency state is re-read, so a previously accepted request remains replayable after its prerequisites change.
- Snapshot-native DuckDB delivery now uses that dependency scheduler for a bounded distributed execution slice. One immutable multi-file input and row-local transforms compile into up to 100 exact manifest shards; independently claimable `pipeline.duckdb.partition` jobs publish hash-addressed Parquet fragments, and one blocked `pipeline.duckdb.finalize` job validates every fragment before lease-fenced publication. Unsafe global operations are rejected, automatic mode falls back explicitly, final output lineage names every shard, and idempotent replay returns the same job graph and snapshot. The focused test proves the semantics locally and the deployable daemon supports the job types; true physical multi-host/object-network evidence remains an open gate.
- React Pipeline Builder now exposes the snapshot-native execution contract without raw JSON. Users can choose the general builder runtime or typed DuckDB plans, select automatic, single-worker, or partition-worker delivery, bound the shard count, and inspect durable child-job status plus finalizer evidence. Execution strategy and fallback reasons persist through ordinary job polling instead of existing only on the enqueue response. A visible-control Chrome acceptance builds a four-file fixture, selects three partitions, completes every shard and finalizer, and queries the published output; the page remains axe-clean and overflow-free at all four required viewport widths.
- Governed agents now support real resumable task graphs instead of treating one leased transaction as a multi-step task. `/api/v1/agents/{id}/task-graphs` snapshots selected tools and retrieval configuration, queues one context job, independently retryable tool jobs, and one dependency-blocked synthesis coordinator. Tool stages cannot mutate ontology state; synthesis alone creates deterministic approval requests and final run/audit/Ops evidence after validating every stage identity. Cascade cancellation preserves explicit cancellation status, targeted retry resumes failed stages without repeating successful tools, and idempotent enqueue returns the same graph. The React agent panel uses task graphs by default, displays every context/tool/synthesis stage, and retains the original single-job mode for compatibility.
- External evaluator acceptance now has a machine-verifiable protocol instead of an informal roadmap sentence. A completed project-owned workflow can export a privacy-preserving bundle binding its immutable source fingerprint, migration head, release commit, OIDC principal hash, seven server-derived workflow stages, decision report hash, and canonical bundle hash. The Command Center exposes the export and corrective qualification reasons; the standalone verifier rejects tampering, sample/unprovenanced data, incomplete stages, local auth, unpinned releases, and duplicate team/organization/deployment/principal/dataset submissions. No external completion is claimed until strict validation receives two independent qualifying bundles.
- API v1 compatibility now covers the assembled product surface instead of a small hand-maintained subset. The application installs 898 additive typed aliases backed by the exact legacy endpoint contract, defers 37 method/shape collisions to explicit v1 implementations, exposes a runtime manifest, preserves middleware and dependency authorization, and deliberately excludes only browser/auth/probe/deployment surfaces. The runtime-aware route audit resolves router prefixes and ratchets the unversioned population; behavioral tests prove cross-path state, project denial, OpenAPI typing, unique operation IDs, and permission parity.
- Advanced snapshot plans use order-independent named parameters and SQL-native IDs, title normalization, pivot/unpivot, windows, validation barriers, point/radius transforms, MGRS, polygon geofences with holes, and spatial joins; deterministic plan/data failures terminate without infrastructure retries.
- Production OIDC now supports an explicit private back-channel for discovery, token, and JWKS traffic while preserving the public issuer for browser redirects and ID-token validation. Production request paths no longer perform opportunistic schema DDL, preventing nested connection acquisition under load.
- Visual builders now use an authenticated, origin-restricted, cursor-resumable WebSocket over the authoritative PostgreSQL collaboration log. The React client tracks the last processed cursor and reconnects with bounded exponential backoff; idempotent mutations remain on the transactionally fenced HTTP command path.
- An Ed25519 plugin registry now binds immutable project versions to organization trust keys, exact ZIP hashes, declared operations/capabilities, activation and revocation state, request-hashed execution evidence, builder catalogs, and audit/outbox events. Development execution is constrained to a child process; production rejects that mode and executes through a digest-pinned, non-root, read-only, capability-dropped, network-denied OCI boundary. A dependency-free versioned SDK supplies typed connector, transform, widget, ontology-package, and model-provider result contracts. The real container rehearsal verifies successful SDK execution plus filesystem, network, subprocess, and incompatible-version denial. Portable project snapshots re-verify signatures and exact bundle bytes, while production backup/restore can carry an independently checksummed plugin archive.
- The pilot recovery path now uses a dedicated bearer-protected internal protocol that writes idempotent source durability markers into the transactional outbox, rejects source/recovery URL aliasing, and verifies both database and runtime migration heads after restore. A host scheduler writes hash-chained availability, RPO, and RTO journals, establishes a prior recovery point before rehearsal, records failed attempts, performs authenticated post-restore writes, and cleans up isolated targets. A cross-platform reference driver authenticates its database/snapshot/plugin manifest with a separate HMAC key and restores into a distinct Compose project with fresh volumes. The Control Panel exposes the three independent gates without treating partial wall-clock collection as acceptance.
- Seven-day pilot supervision now survives session and host restarts without inheriting an interactive shell: the CLI accepts an explicit durable evidence root, protected runtime environment file, and separate token file; Windows registration rejects broadly readable ACLs; and the Linux systemd installer verifies the service account can read a mode-restricted token before enabling the supervisor. Neither startup definition embeds the recovery credential in its command line.

## Verified

- 211 isolated backend scripts pass in one sequential run, covering the API v1 compatibility and route-coverage ratchets plus partitioned local and S3-compatible snapshot manifests, direct multi-file query execution, immutable-content checks, advanced spatial transforms, strict ontology mixed-load, physical-backup, replica-failover, OIDC back-channel behavior, production runtime-DDL guards, identity-scale and WebSocket-chaos contracts, signed plugin lifecycle/SDK/isolation/recovery, version-bound downstream ontology contracts, project-scoped Decision Intelligence, transactional ontology object-change delivery, durable event-to-stream routing, replay-safe inner/outer two-stream interval correlation, watermark-safe bounded join-state compaction, lifecycle-aware and checkpoint-resumable industrial onboarding, authenticated isolated recovery, the concrete PostgreSQL pilot driver, pilot scheduling and evidence integrity, clean ontology SDK installation, CI wiring, and conformance evidence. The governed import regression proves that strict production delivery rejects an unpublished generated object contract, then succeeds only after a captured change set is validated, approved, and published; the supervisor deployment regression proves restart-safe protected configuration on Windows and systemd; and the snapshot publication regressions prove stale-worker denial, real process-loss recovery into an independent cache, exactly-once fenced output, and concurrent PostgreSQL output serialization.
- Docs conformance covers 72 rows and 308 executable assertions with no required P0 gap after adding immutable downstream ontology bindings, human-visible compatibility health, exact-set breaking-change acknowledgement, unique generated-client OpenAPI operation IDs, real OCI, governed egress, installable SDK compatibility, project-owned industrial onboarding, durable object-change routing, replay-safe inner/outer cross-stream correlation, bounded join-state evidence, and authenticated isolated pilot recovery evidence.
- Concurrent PostgreSQL interval processing and maintenance now serialize on the processor lease: two workers produce one exact receipt per input, while two compaction calls remove each expired input once and retain every exact pair and materialized output. The S3 snapshot cache concurrency path also passed five consecutive eight-reader rehearsals after replacing a race-prone Windows canonicalization check with deterministic relative-path validation.
- SQLite and clean PostgreSQL migration chains reached `0042_stream_outer_joins`; all 28 focused migration scripts pass, including legacy partial-schema compatibility and the outer-join downgrade/upgrade boundary.
- A clean PostgreSQL 16 migration passed `0031 -> 0032 -> 0031 -> 0032` and finished with 305 tables. The earlier collaboration rehearsal confirmed that concurrent non-overlapping proposals serialize and rebase while competing edits to the same node produce one applied revision and one durable conflict.
- Two consecutive real-socket rehearsals across two Uvicorn replicas applied 20 simultaneous stale-base, target-disjoint edits as contiguous revisions with zero lost updates. Acknowledgement p95 measured 206.458 ms and 224.866 ms against the 250 ms gate; 200-read batches completed in 0.567 s and 0.623 s with identical committed state.
- A clean PostgreSQL benchmark passed at 100,000 objects/500,000 links and at 1,000,000 objects/5,000,000 links. The larger run measured exact lookup p95 12.095 ms, range/order p95 47.778 ms, and two-hop graph p95 22.509 ms over 77 nodes/100 edges, with physical property/source/target index evidence.
- The refreshed strict PostgreSQL profile passed with 10,000,000 objects and 50,000,000 links. Exact lookup measured 8.718 ms p95, range/order 11.830 ms p95, and bounded two-hop expansion 13.721 ms p95 over 77 nodes/100 edges. Relations occupied 7.25 GB and 28.93 GB including indexes.
- The strict mixed ontology profile then committed 100,000 object mutations and matching temporal events in 500 bounded transactions while eight readers completed 88,980 typed API samples at 53.635 ms p95. Transactions measured 1,117.156 ms p95 and sustained 213.932 writes/s; rollback and post-write index checks passed. Vacuum/analyze plus a real PostgreSQL process restart preserved 10M objects, 50M links, and 200,000 accumulated mixed events, retained the index plan, and recovered in 2.714 seconds.
- A 36.59 GB `pg_basebackup` copied the live strict fixture into a fresh Docker volume in 50.917 seconds; an independent PostgreSQL instance became query-ready in 1.144 seconds with identical migration, object, link, event, and index-plan state. A separate physical standby replayed a committed object/event probe in 0.015 seconds at the same WAL LSN and promoted in 0.686 seconds after source termination with no committed-probe loss.
- The expanded strict snapshot-native pipeline profile processed 10,000,000 fact rows across eight immutable partitions through stable IDs, point/radius evaluation, MGRS encoding, polygon geofencing, a dimension join, and a partitioned window before atomically writing 20 aggregates into 20 Hive-style output partitions without hydrating bulk records into Python. Preview p95 measured 3,663.405 ms and delivery 4,046.026 ms; per-file integrity manifests and replay remain duplicate-safe.
- A real MinIO rehearsal created the bucket, registered two connector-style Hive-partitioned Parquet objects through the public API, recovered the key-path field, executed the DuckDB plan through the governed local cache, published two partitioned output objects, and queried the output through the public snapshot API. The deterministic S3 test also covers exact objects, prefix limits, cross-bucket rejection, concurrency, quota/LRU controls, and remote manifest corruption without external-service dependence.
- The MinIO object-storage reference profile registered and queried 1,000,000 rows across eight objects. On local Docker loopback, registration measured 281.585 ms, cold query 128.102 ms, warm query p95 87.243 ms, cold pipeline preview 351.269 ms, and warm pipeline p95 49.161 ms. This is a local regression baseline, not WAN or managed-cloud evidence.
- Ten concurrent PostgreSQL publishers produced 100 contiguous unique stream sequences; two concurrent processors produced exactly 100 receipts without duplicate work.
- Two concurrent PostgreSQL event routers consumed one 100-event binding into exactly 100 contiguous stream records and 100 unique event receipts; the second router observed the committed cursor and produced no duplicate work.
- Two concurrent PostgreSQL stream workers raced over 50 left and 50 right inputs; one run consumed all 100 records and emitted exactly 50 unique interval pairs, while the second observed an empty backlog without duplicate receipts or output rows.
- Full outer interval joins now finalize unmatched rows only after a strictly safe opposite-side watermark, persist immutable receipts, reject watermark regression, preserve exact-boundary matches, and serialize concurrent PostgreSQL finalizers. A bounded one-output regression proves older matched rows are removed by the SQL predicate before limiting and cannot starve an eligible unmatched row.
- A PostgreSQL connection-loss rehearsal now interrupts an observed outer-receipt INSERT before commit, proves the watermark/receipts/materialized output roll back together, then emits 60 retained rows exactly once after recovery. The chaos gate independently requires current-head inner-pair and outer-finalization rehearsals instead of allowing either semantic to stand in for both.
- A real Kafka broker rehearsal produced and consumed the expected envelope with matching event ID, partition, and offset. A separate stop/restart rehearsal persisted `RETRY` during interruption and delivered the same receipt on attempt two after recovery.
- Frontend TypeScript and production build passed; production dependency audit reported zero vulnerabilities.
- Focused Playwright acceptance created, approved, and applied a governed Workshop proposal through the React review panel. A second visible-control acceptance compiled a promoted project dataset into a namespaced asset ontology and deterministic risk workflow, then verified the hydrated and high-risk counts in Command Center.
- 100 responsive/accessibility/browser checks passed across 375, 768, 1280, and 1600 pixel projects; 96 viewport- or workflow-specific cases were intentionally skipped by their configured project conditions. The desktop suite completes the governed Command Center workflow entirely through visible controls, downloads the final Markdown report, validates human signed-extension onboarding without exposing bundle payloads, and now proves downstream ontology contract health plus breaking-change acknowledgement.
- Production Compose rendered successfully and the multi-stage application image built with DuckDB, Arrow, and S3 dependencies.
- A fresh production image reports migration head `0042_stream_outer_joins`, rebuilds the TypeScript/Vite application, and exposes the watermark-finalization and outer-receipt routes through generated OpenAPI.
- A live reference-driver rehearsal used the tested production image in two independent Compose projects. It backed up a current-head durability mark with authenticated database, snapshot, and plugin metadata, destroyed and recreated only the recovery volumes, waited for final PostGIS initialization, restored in 19.094 seconds, read the source mark from the isolated API, and committed an authenticated write probe with matching database/runtime heads. Both projects and all rehearsal volumes were removed afterward. This proves the executable recovery path once; it is not repeated RTO or seven-day RPO evidence.
- A clean production acceptance rehearsal passed at `0042_stream_outer_joins`. Real Keycloak authorization-code/PKCE and RBAC passed before and after an API restart; the current-head run provisioned 200 viewers at concurrency 20 and authenticated all 200 distinct identities at concurrency 10 across two API replicas, with 200 forbidden mutations, 200 unique principals, and login p95 1,716.151 ms against a 15,000 ms gate. The bounded browser concurrency avoids Chromium resource churn after the container/plugin stages; the separate collaboration gate still exercises 20 concurrent editors and 200 readers. The same acceptance profile covers governed ontology publication before pipeline delivery, isolated plugin executor loss/recovery, job idempotency, abandoned-worker recovery, serialized migrations, and fresh-volume PostgreSQL backup/restore.
- A separate PostgreSQL WebSocket chaos rehearsal terminated one API replica, resumed on its peer, restarted the failed replica, and resumed again with three ordered command events, zero duplicates, zero missed events, final revision 4, and maximum reconnect 209.067 ms against a five-second gate. A clean Keycloak production run then passed the authenticated browser WebSocket before and after API restart and retained the 200-identity gate.
- Tier B currently passes 7 of 10 gates at `0042_stream_outer_joins`: ontology scale, mixed workload, pipeline scale, collaboration, identity, durability, and chaos. Current evidence covers 10 million objects/50 million links with object lookup p95 9.8 ms and two-hop graph p95 15.709 ms; eight-reader mixed load with 100,000 writes and read p95 57.058 ms; a 10-million-row snapshot pipeline delivered in 3,828.364 ms; 20 editors across two replicas with acknowledgement p95 187.34 ms; 200 distinct PKCE principals and 200 mutation denials; fresh-volume restore plus promoted-replica preservation; and independent collaboration, inner-stream, and outer-stream recovery. Availability, repeated RPO, and scheduled RTO remain missing until one migration-scoped seven-day pilot window completes. The evidence-corpus ratchet remains capped at 10 unprovenanced files.
- The strengthened pipeline reference rehearsal also passes a 10-million-by-10-million-row equi-join, a 20-million-row schema-aligned union, and a 512-column pivot. Complex preview completed in 1,347.479 ms and partitioned delivery in 1,912.904 ms while materializing only two result rows in Python; replay returned the exact committed snapshot. CI exercises the same topology with one million rows per large input.
- Snapshot-native delivery now separates expensive computation from a lease-fenced publication transaction. Output namespaces are serialized before snapshot numbering and promotion, PostgreSQL locks absent and existing assets with a transaction advisory lock, expired workers remove staging data without publishing, and the replacement worker can requeue and claim the job in the same poll. Delivery audit evidence records the execution job and fence state.
- The schedule-critical evidence path supports two explicit single-writer modes: a container availability observer or a host scheduler for complete availability/RPO/RTO collection. Persisted migration-scoped runs, hash-chained journals, durable tail anchors, restart gap backfill, distinct recovery targets, authenticated source markers, restored-head checks, and failure-preserving rehearsal records prevent optimistic evidence. Control Panel distinguishes pilot availability from job success and shows sample/rehearsal coverage and worst observed RPO/RTO. Unit and real-HTTP protocol tests pass, but no seven-day availability window or repeated production restore schedule has accrued; those gates remain unclaimed.

## Remaining Ending Gates

- Extend the achieved 10-million-object/50-million-link mixed, fresh-volume backup, and replica-failover gates with longer-duration saturation, repeated scheduled recovery, and partitioning only where measurements justify it.
- Expand the achieved 10-million-row partitioned input/output, 9,218-position polygon, 10-million-by-10-million join, 20-million-row union, 512-column pivot, latency-injected four-worker MinIO reference, and OIDC-authenticated independent-Docker-daemon worker-loss recovery to the intended managed object-store network, true multi-host caches, and sustained repeated worker-loss recovery.
- Sustained multi-partition throughput and broker-assigned processor partitions beyond the verified local/PostgreSQL inner/outer interval joins, transaction-interruption recovery, bounded join-state compaction, and Kafka interruption paths.
- Long-duration collaboration presence, rolling deployments, and repeated network-partition chaos beyond the verified authenticated WebSocket process-loss/restart path.
- Sustained 99.9% pilot availability evidence, repeated five-minute RPO sampling, and scheduled 30-minute RTO rehearsals beyond the verified clean production acceptance and fresh-volume restore.
- Promote the now-reproducible npm and Python wheel SDK artifacts to an external organization registry when one is configured. Local governed downloads, SHA-256 evidence, audit attribution, and clean offline consumer installation are verified; production OIDC service identity, dedicated executor loss/recovery, exact-destination egress, direct-bypass denial, and signed custom-CA HTTPS are also rehearsed with credential-redacted evidence.
- Have at least two independent external evaluator teams repeat the now-verified no-code project-owned Connect-to-Report workflow with their own organization data, and retire compatibility-only pages after equivalent route coverage is documented.
- Two independent external evaluator teams completing the workflow from fresh-install documentation.

No completion status should be assigned to the active goal until every declared ending condition has objective evidence.
