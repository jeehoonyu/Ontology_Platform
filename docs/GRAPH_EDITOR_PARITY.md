# The original's graph controls, against the pipeline canvas

Generated by `oms/audit_graph_editor.py`. Do not edit by hand — the gate
regenerates this and fails if it disagrees with the source.

**0 of 15** controls met, each operated by a named browser test. 14 gaps, 7 of them with a handler that does part of the job; 1 not taken, with the reason.

The controls were read from the original's public documentation (`GOAL_GRAPH_2026-09-23.md`). Documentation describes a capability and proves nothing about any enrollment, so this is not a parity claim: it is a list of what a person who has used the original will reach for here, and what happens when they do.

| Control | The original | Ours | Handler | State | Evidence |
| --- | --- | --- | --- | --- | --- |
| `lasso` | Drag Select Mode; Shift+drag from panning mode lassos nodes | click selects; Shift+click extends (M5) | `selectNode` in `workspaces/PipelineBuilder.tsx` | **gap** | no lasso |
| `select-all` | Select all nodes; Ctrl+A | nothing | — | **gap** | no way to select every node |
| `select-family` | Ctrl+D selects children, Ctrl+E selects parents | nothing | — | **gap** | no way to select along the edges |
| `remove-selected` | Remove the selected nodes | Delete node, one node at a time | `removeNode` in `workspaces/PipelineBuilder.tsx` | **gap** | deletes one node, not a selection |
| `layout` | Layout: evenly disperse and organise the graph; grid snapping | nothing on this canvas; PlatformGraph has Auto layout | — | **gap** | no layout command on the pipeline canvas |
| `zoom-fit` | Zoom in, out and fit; Up Arrow fits | Zoom in, Zoom out and Fit to view buttons | `onZoom` in `components/canvas/PipelineCanvas.tsx` | **gap** | no key fits |
| `copy-paste` | Ctrl+C / Ctrl+V copy and paste nodes, across pipelines | nothing | — | **gap** | no copy and no paste |
| `hide` | Ctrl+H hides the selection, Ctrl+K shows every hidden node | nothing | — | **gap** | nothing can be hidden |
| `search` | Ctrl+F opens Search pipeline | nothing | — | **gap** | no search of the graph |
| `organise` | Legend and colour groups; folders; text nodes | a legend of the four node categories | — | not taken | deliberately not taken: organisation for a large graph, and the bootstrap pipeline has under ten nodes (GOAL_GRAPH, what is not taken) |
| `connect` | Drag an output circle to an input circle to connect | an insert control on each edge, which adds a new node after the selected one | `insertAfter` in `workspaces/PipelineBuilder.tsx` | **gap** | no port drag, and nothing connects two existing nodes |
| `context-menu` | Right-click a node: Copy, Paste, Open | a menu under the node selected by a click, with inserts and Delete | `onContextInsert` in `components/canvas/PipelineCanvas.tsx` | **gap** | no right-click menu |
| `undo-redo` | Undo and Redo in the top toolbar | Undo move, one step, moves only | `undoMove` in `workspaces/PipelineBuilder.tsx` | **gap** | no redo, and adds and deletes are not undone |
| `save-state` | Explicit Save, a filled Saved state, View changes for unsaved work | positions save on drop; node configuration is held in drafts | `moveNodes` in `workspaces/PipelineBuilder.tsx` | **gap** | nothing says whether there is unsaved work |
| `hotkeys` | Help, View hotkeys | one hotkey, Escape clears the selection | — | **gap** | no reference, and one key |
