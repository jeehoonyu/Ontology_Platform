# OntologyOS Industrial Operational Intelligence Goal

Version 2, stated 2026-08-03 on `codex/builder-ux-production`.

This goal completes. [`GOAL_STANDING.md`](GOAL_STANDING.md) does not: it is the standing
discipline that keeps the claims below from outliving their proof once this document is
satisfied, and it is what the three defects recorded here were found by.

This goal supersedes the informal goal narrative used by
[`GOAL_ACCEPTANCE_2026-07-28.md`](GOAL_ACCEPTANCE_2026-07-28.md) and
[`GOAL_PROGRESS_2026-07-29.md`](GOAL_PROGRESS_2026-07-29.md). Those documents remain
authoritative as **evidence records**; this document is the **contract** they are
measured against. It does not claim Palantir source, API, or infrastructure
compatibility.

## Outcome

An operations team outside this project, starting from a fresh install and public
documentation alone, connects their own industrial data and reaches an approved,
evidence-linked decision report without writing code or editing raw JSON, and repeats
that run intact across an upgrade and a restore.

Everything below is a means to that outcome, not a substitute for it.

## Center of gravity

Every object, contract, action, and decision on that path is bound to an immutable
ontology revision. Every state change is append-only and carries actor, source,
transaction time, valid time, changed fields, ontology revision, and evidence
references. Breaking ontology changes are classified and acknowledged before they touch
an active schema, or they roll back atomically without drifting the live contract.

## In scope

1. The human-guided Connect-to-Report workflow, driven entirely through visible controls.
2. Normalized, project-scoped ontology contracts with temporal object change events.
3. Typed object and graph querying with filters, aggregation, temporal and spatial reads,
   masking, bounded expansion, and stable cursor pagination.
4. Content-addressed immutable dataset snapshots and a pluggable pipeline execution
   boundary with compiled plans, field lineage, and deterministic identities.
5. Governed provider-neutral AI that may retrieve context and propose actions but never
   unilaterally commits a governed mutation.
6. Collaborative visual builders with durable command receipts, revision fencing, and
   review/approval workflows.
7. Production observability, deployment, backup, restore, and failure recovery.
8. An additive, compatibility-preserving `/api/v1` surface.

## Explicitly out of scope

- Multi-region or internet-scale operation.
- Managed-service parity with any commercial platform.
- Palantir source or infrastructure compatibility.
- More than 50 concurrent users.
- Hosted model inference as a default execution path.

Work that advances an out-of-scope item does not advance this goal.

## Ending conditions

Completion is tiered. Each tier is claimed only against durable artifacts committed to
this repository, never against narrative. No tier inherits a prior tier's evidence
without re-execution on the current migration head.

### Tier A — Feature complete — **Blocked**

*5 of 8 sub-conditions met, 1 unmet, 2 unavailable — postgres and docker, neither present
on this machine. Computed by `oms/audit_tier_a.py`, which gates against regression.*

The note that stood here for two weeks — *no artifact claims the tier complete* — was
accurate and misleading in the same breath. Nothing claimed the tier, and several of its
parts were demonstrably satisfied with nobody recording it. Measured 2026-08-18:

| Sub-condition | State |
| --- | --- |
| validation matrix has no `PARTIAL` or `MISSING` row | **met** — 73 rows: 60 LOCAL_ANALOG, 11 MATCH, 2 INTENTIONAL_DIFFERENCE |
| documentation conformance passes | **met** |
| alembic applies twice on SQLite | **met** — at `0042_stream_outer_joins` |
| frontend typechecks, builds, audits clean | **met** — `npm audit --omit=dev --audit-level=high`: 0 vulnerabilities |
| backend suite passes sequentially | **met** — 231 scripts, read from the last `verify.py` run at this head |
| browser matrix, skips attributable | **unmet** — one failing test, declared in `known_failing`: the deploy race that reports SUCCEEDED before the job result is complete. Recorded met once, from a run where the race did not fire. Tier A is a completion claim, so quarantined debt is still a failure here. |
| alembic on postgres, with downgrade | **unavailable** — needs postgres |
| production compose renders, images build | **unavailable** — needs docker |

`verify.py --full` closed the last two that were resolvable here: 20 of 20 in 41.4 minutes,
covering 231 suite scripts, the browser matrix across four viewports, and the suite-cost
census. What remains is not work — it is two services this machine does not have.

`unavailable` is not a pass. It is counted apart so that the tier can never be claimed from
a run that could not look at half of it, and each one states what it needs.

One number here was wrong before it was computed: a first count by hand reported two
`MISSING` and one `PARTIAL` row blocking the tier. Those were the **legend** at the top of
the matrix, explaining what the words mean. The parser reads table cells, and its test
contains a legend to prove it.

- Every P0 and P1 row in [`VALIDATION_MATRIX.md`](../foundry-docs/VALIDATION_MATRIX.md)
  is `MATCH`, `LOCAL_ANALOG`, or `INTENTIONAL_DIFFERENCE` with a recorded rationale.
  Zero `PARTIAL` and zero `MISSING` rows at any priority.
- `python oms/validate_docs_conformance.py` passes with no required P0 gap.
- The full backend script suite passes in one sequential run on the current tree.
- The Alembic chain applies twice idempotently on SQLite and PostgreSQL and survives
  downgrade/upgrade verification at the declared head.
- Frontend typecheck and production build pass; `npm audit --omit=dev
  --audit-level=high` reports zero vulnerabilities.
- The Playwright evaluator matrix passes across the 375, 768, 1280, and 1600 pixel
  profiles, including WCAG serious/critical checks, with every skip attributable to a
  declared project condition rather than a failure.
- Production Compose renders and the multi-stage application, plugin executor, and
  egress proxy images build.

### Tier B — Production pilot accepted — **Blocked**

*7 of 10 gates PASS at head `0042`. The three that do not are blocked, and not on effort:*

| Gate | Why it is blocked |
| --- | --- |
| `availability` | needs a **7 consecutive day** observation window. That pilot was stopped by an explicit decision on 2026-08-14; it is not reopened without one. |
| `rpo` | needs a recovery rehearsal against replicated infrastructure |
| `rto` | needs the same |

Recorded as blocked rather than open because the distinction matters to whoever reads the
next step: an open condition is work someone can start, and none of these is. The other
seven gates hold at this migration head.

Claimed against declared numeric gates, each backed by a machine-readable evidence file
in `docs/`.

| Gate | Threshold |
| --- | --- |
| Ontology scale | >= 10,000,000 objects and 50,000,000 links with bounded exact-lookup, range/order, and two-hop expansion p95 |
| Mixed workload | Concurrent typed reads during bounded transactional writes, with rollback atomicity and retained index plans |
| Pipeline scale | >= 10,000,000 partitioned input rows to partitioned immutable output without bulk row hydration |
| Collaboration | 20 editors, two replicas, command acknowledgement p95 < 250 ms, zero lost updates |
| Identity | 200 distinct PKCE identities under the declared login p95 gate, with mutation denial enforced server-side |
| Availability | Sustained 99.9% pilot availability over a declared measurement window |
| RPO | <= 5 minutes, sampled repeatedly rather than once |
| RTO | <= 30 minutes, rehearsed on a schedule |
| Durability | Fresh-volume backup/restore and replica failover with zero committed-record loss |
| Chaos | Network-partition and process-loss recovery for collaboration and cross-stream processing, with zero missed or duplicated events |

### Tier C — Independently confirmed — **Blocked**

*follows Tier B, which is blocked.*

- At least two external evaluator teams, unaffiliated with development, each complete the
  Connect-to-Report workflow on their own data from fresh-install documentation only.
- Defects they report are triaged into this goal's severity ledger before any claim.
- SDK artifacts are consumed from an external organization registry rather than a local
  governed download.
- Compatibility-only routes are retired once equivalent typed route coverage is
  documented.

The documenting half of that last condition is done:
[`ROUTE_COVERAGE.md`](ROUTE_COVERAGE.md), generated by
`python oms/audit_route_coverage.py`. It reports 35 dual-registered handlers that a typed
`/api/v1` route already covers and which are therefore retirable, 8 typed-only handlers,
and 958 with no typed twin.

That last number is the finding. The typed surface covers 43 of 1,001 handlers, so
retiring the 35 removes duplication rather than making the API typed. The condition as
written is satisfiable at a much smaller scope than "retire the compatibility surface"
suggests, and reading it the larger way would turn a cleanup into a rewrite.

## Severity gate

Defect severity is distinct from the requirement priority column in
[`VALIDATION_MATRIX.md`](../foundry-docs/VALIDATION_MATRIX.md). The two must not be
reported as one number.

- **P0 defect** — blocks the outcome path, or loses or corrupts committed data,
  evidence, or tenant isolation.
- **P1 defect** — degrades the outcome path or its governance guarantees, with a
  documented workaround.
- **P2 defect** — everything else.

No tier is claimed while an unresolved P0 or P1 defect is open against its scope. Any
downgrade from P0 or P1 to P2 is recorded in that tier's acceptance record with owner,
date, and rationale. Environmental blockers outside the product are recorded as
conditions with a named owner, not silently absorbed.

## Non-completion rule

No tier is marked complete while any of its conditions lacks objective evidence. Partial
progress is reported as a measurement, never as a status word.

## Tier status

Measured on 2026-08-03 against `codex/builder-ux-production` with uncommitted working-tree
changes present.

### Tier A

Re-measured 2026-08-04 at head `0038_explicit_schema_baseline`. The earlier readings in
this table were taken at `0037` and did not survive the head advancing, so they were
re-executed rather than carried forward.

| Condition | Status | Measurement at head `0038` |
| --- | --- | --- |
| Matrix has no `PARTIAL` or `MISSING` row | PASS | 72 rows: 11 `MATCH`, 59 `LOCAL_ANALOG`, 2 `INTENTIONAL_DIFFERENCE`; zero `PARTIAL`, zero `MISSING` |
| Docs conformance, no required P0 gap | PASS | `validate_docs_conformance.py`: required P0 target satisfied. `test_docs_conformance.py`: 276 assertions, stable across 12 runs after GOAL2-002 |
| Backend suite passes sequentially | PASS | 186 of 186 scripts, 0 failed, 809 s, one sequential run on the post-fix tree |
| Migration upgrade/downgrade chain | PASS | Both dialects: applied twice idempotently, then `upgrade head -> downgrade base -> upgrade head`. PostgreSQL 16 reaches 272 tables and 887 indexes. A database stamped at `0037` with zero tables converges to the same schema |
| PostgreSQL runtime verifies | PASS | All seven CI `verify_*_postgres` scripts, plus the cross-stream partition rehearsal |
| Frontend typecheck, build, production audit | PASS | `tsc --noEmit && vite build` exit 0, 240.57 kB main entry chunk; `npm audit --omit=dev --audit-level=high` reports zero vulnerabilities |
| Playwright matrix across four viewports | PASS | 208 cases at 375, 768, 1280 and 1600 px: 100 passed, 108 skipped, 0 failed, 1.8 min. The suite migrates to head before running, so it exercises `0038` end to end. Every skip carries a declared condition |
| No unresolved P0 or P1 defect | PASS | Ledger: GOAL2-001, -003, -005 fixed; GOAL2-004 open at P2 |

| Compose render and image builds | PASS | Production and rehearsal Compose models render clean. All four images build: application 643 MB, plugin executor 336 MB, egress proxy 177 MB, plugin sandbox 177 MB |

**Tier A was claimed as met at head `0038` and that claim is retracted.**

The claim was made on 2026-08-05 with every listed condition measured and the ledger
showing no unresolved P0 or P1. Hours later GOAL2-006 was found: interface-scoped object
queries returned committed instance data across project boundaries. Tenant isolation is
named in the P0 definition, and no tier may be claimed with an unresolved P0, so the tier
was not met when it was recorded as met.

The conditions themselves did not change and none of the measurements was wrong. What was
wrong was reading "no unresolved P0 in the ledger" as "no P0 exists". The ledger records
what has been found, and Tier A's condition list does not include a security review; a
clean ledger is therefore evidence of absence only to the extent that someone has looked.
GOAL2-006 was found by following up a loose end already written down in the Stage 1
notes — that `OntologyInterface` carries no `project_id` — which had been filed as
untidiness rather than investigated.

**Restored 2026-08-05.** GOAL2-006 is fixed and every condition was re-executed on the
post-fix tree rather than carried across the change: backend suite 186 of 186 in 809 s;
matrix zero `PARTIAL` and zero `MISSING` across 72 rows; docs conformance with no required
P0 gap; frontend typecheck and build clean with zero production vulnerabilities; the
Playwright matrix 100 passed, 108 skipped by declared conditions, 0 failed across four
viewports; both Compose models rendering; and all four images building. The severity
ledger carries no unresolved P0 or P1.

The claim is worded more carefully this time. A clean ledger means **nothing has been
found so far**, not that nothing is there. Tier A's conditions test behavior that is
specified; they do not search for behavior nobody specified, which is where GOAL2-006
lived. The tier is met as defined, and that is a narrower statement than "the system is
sound".

Three defects were found and fixed while closing this tier — GOAL2-001, GOAL2-003, and
GOAL2-005 — and none was found by adding a feature. Each surfaced by asking a claim to
prove itself: whether the suite really passed in one sequential run, whether the downgrade
chain had ever been walked, and whether the migration head identified the schema it named.

One limit on the claim. These runs were sequential on one development host, not a single
CI run. The GitHub-hosted runner blocker recorded in
[`GOAL_ACCEPTANCE_2026-07-28.md`](GOAL_ACCEPTANCE_2026-07-28.md) is unchanged and remains
with the repository administrator; it is a condition with a named owner rather than a
product defect, and it is not absorbed silently.

Execution for the next tier is planned in
[`GOAL_TIER_B_2026-08-03.md`](GOAL_TIER_B_2026-08-03.md). Tier B stands at 0 of 10 gates:
advancing the head to `0038` correctly invalidated the chaos evidence earned at `0037`.

### Tier B evidence obtained while closing Tier A

| Gate | Threshold | Measurement |
| --- | --- | --- |
| Collaboration | 20 editors, two replicas, ack p95 < 250 ms, zero lost updates | PASS at 241.605 ms p95 (p50 150.707 ms), 20 editors, 2 replicas, 0 lost updates, 20 unique contiguous revisions, 200-reader batch 0.628 s |

This gate now clears by 8.4 ms. It was measured at 206.458 ms and 224.866 ms in the
2026-07-29 record, so the margin is narrowing across runs on different hosts and should
not be treated as comfortably met.

## Defect ledger

| ID | Severity | Area | Status | Summary |
| --- | --- | --- | --- | --- |
| GOAL2-001 | P1 | Collaborative builders | FIXED | A collaboration or builder command response could report a concurrent writer's revision, lock version, and state instead of the revision the request allocated |
| GOAL2-002 | P2 | Verification harness | FIXED | `test_docs_conformance.py` selected an outbox event by list order and failed about one run in four |
| GOAL2-003 | P1 | Migrations and recovery | FIXED | The SQLite downgrade chain could not reach base: six migrations dropped an indexed column without dropping its index first |
| GOAL2-004 | P2 | Tier B measurement | DIAGNOSIS CORRECTED | The collaboration gate was attributed to a noisy single-observation p95; six runs at head 0038 spread 6.791 ms, so host contention was the dominant factor, not the estimator |
| GOAL2-005 | P1 | Migrations and evidence provenance | FIXED | Migration head did not identify the schema: 215 of 271 tables reached a database only through the baseline's `create_all`. Now 0 |
| GOAL2-006 | P0 | Tenant isolation | FIXED | Interface-scoped object queries returned committed instance data from every project in the deployment |
| GOAL2-007 | P0 | Read path at scale | FIXED | Every object-set read except the unfiltered one materialized the entire object type: 21.9 GB measured for one filtered read at 10M on Postgres. Now 1.6 MB. Latency for facets and spatial queries is still linear and tracked as B3/B4 |
| GOAL2-010 | P1 | Read path, wasted total | FIXED | `/object-explorer/query` computed an exact `count(*)` over every match and discarded it -- `result_count` is the page size. 567.009 ms to 7.144 ms at 10M. The first diagnosis, that the path could not use the property indexes, was wrong: it compared an unindexed property against indexed ones |
| GOAL2-009 | P1 | Read path, session integrity | FIXED | Streaming expunged rows to bound memory, detaching instances the caller still held, so a read could silently sever a pending write. The scan now selects columns and never enters the identity map |
| GOAL2-008 | P1 | Semantic rendering reach | FIXED | The renderer handled 13 of 13 semantic base types and one workspace of twenty-two consumed it. `DataTable` now accepts specs; the Operational Map and the decision timeline draw by declared type; a reach ratchet replaces the vocabulary count |

### GOAL2-001

`db.commit()` expires the artifact ORM row, so `_artifact_dict` re-selected the artifact
head when building the response. A writer that committed first, then lost the race to
finish its response, was handed the competing writer's `current_revision`,
`lock_version`, and `state`.

Committed data was never corrupted: every revision row remained unique and correctly
numbered, and each `ArtifactCommandReceipt` recorded the correct revision. The defect was
confined to what the caller was told. It is P1 rather than P0 because it degrades the
collaborative-authoring guarantee without losing committed data or evidence: a client
that adopts the returned `lock_version` as its next base can skip its own revision and
silently adopt a peer's state without any conflict signal.

Reproduced at 6 anomalies in 150 concurrent rounds on SQLite. The PostgreSQL path shared
the same response construction and the same exposure.

Fixed by pinning the response to the revision the request allocated. `_artifact_dict`
accepts optional `pinned_revision` and `pinned_lock_version` arguments, and the three
handlers that allocate a revision capture those values before committing:
`update_artifact`, `apply_artifact_commands`, and `apply_collaborative_commands`. All
other callers are unchanged and still describe the artifact head.

Verified at 0 anomalies in 300 rounds and 600 requests after the fix.

`oms/test_artifact_collaboration.py` previously exercised this race for a single round
and so detected the defect only occasionally, which is why it surfaced as an
intermittent failure rather than a reproducible one. It now repeats the race for 120
rounds on a dedicated `collaborative_race` artifact, and additionally asserts that each
response agrees with its own command receipt. The dedicated artifact keeps the added
revisions from perturbing the event-cursor and receipt-count expectations in the rest of
the test. With the fix reverted the strengthened test failed 3 of 3 runs; with the fix in
place it passed 3 of 3, at a cost of about 7 seconds.

### GOAL2-002

`test_docs_conformance.py` dispatched an ontology object-change event chosen with
`next(...)` over `/api/v1/outbox/events`, then asserted that a binding filtered to
`object_type_ids: ["asset"]` routed at least one event.

That listing is ordered `created_at DESC, id`. The four object-change events it scans —
one `work_order` and three `asset` — are enqueued inside the same second, so the ordering
tie breaks on a random identifier. When the `work_order` event sorted first the test
dispatched it, the asset-filtered binding matched nothing, and the run failed with
`scanned: 1, matched: 0`.

Measured at 2 failures in 8 runs before the fix, which is why it passed standalone and
failed in both full-suite runs. No product behavior is involved: the routing matcher
correctly declined an event whose object type the binding excludes. Severity is P2
because it degrades the verification harness rather than the outcome path, but it blocks
Tier A, which requires the suite to pass in one sequential run.

Fixed by selecting the object-change event whose payload object type matches the binding
the test creates, rather than trusting list order. Verified at 12 passes in 12 runs.

### GOAL2-003

Tier A requires the migration upgrade **and downgrade** chain to pass. The upgrade chain
was already verified on both dialects, but a full downgrade to base had not been
exercised. On SQLite it failed at `0018_project_scoped_ai_evals` with
`no such column: project_id`.

Six migrations added a column, created an index on it, and then dropped the column in
`downgrade()` without dropping the index first: `0007_project_ingestion_runtime`,
`0014_import_project_scope`, `0015_pipeline_project_scope`,
`0016_workshop_project_scope`, `0017_governed_automation_project_scope`, and
`0018_project_scoped_ai_evals`. Migrations `0019` onward, plus `0004`, `0011`, `0030`,
and `0033`, already handled it correctly, so the fix restores an existing convention
rather than introducing one.

The defect is dialect-specific, which is why the PostgreSQL-only CI job never caught it.
PostgreSQL removes dependent indexes by cascade on `DROP COLUMN`. SQLite either rebuilds
the table and recreates the reflected index against a column that no longer exists, or
leaves the stale index behind and rejects the table on next access — the two distinct
error messages observed while walking the chain down.

Severity is P1: SQLite is the default local database, so an operator who upgraded and
needed to roll back could not, which degrades the recovery guarantee. It is not P0
because no committed data is lost and a documented workaround exists — restore from
backup per [`RECOVERY.md`](RECOVERY.md).

Fixed by dropping the `ix_{table}_project_id` index before the column in all six, using
the same guarded pattern `0019` already uses. Verified by a full `upgrade head ->
downgrade base -> upgrade head` round-trip on both dialects, each ending at
`0037_cross_stream_joins`, with PostgreSQL returning to 272 tables.

### GOAL2-004

`verify_collaboration_scale_postgres.py` drives 20 concurrent editors and judges the
acknowledgement p95 against a 250 ms threshold. A p95 over 20 samples is the 19th
value, so the gate is decided by one observation and moves with a single slow request.

Three consecutive runs at head `0037` on one host measured **241.605 ms, 253.002 ms, and
219.812 ms** — a 33 ms spread straddling the threshold. Correctness held in all three:
20 unique contiguous revisions, zero lost updates, every editor acknowledged. Only the
latency threshold moved.

The earlier record shows the same instability across hosts: 206.458 ms and 224.866 ms on
2026-07-29, 241.605 ms earlier on 2026-08-03. The trend is not evidence of regression so
much as evidence that the estimator is too noisy to support a hard threshold.

Recorded as P2 because it degrades the verification apparatus rather than the product,
and no user-facing behavior is implicated. It nonetheless blocks the Tier B collaboration
gate, which is recorded FAIL at the worst of the three observations rather than the best.

### GOAL2-005

`0001_runtime_baseline` upgrades by calling `Base.metadata.create_all(checkfirst=True)`.
The baseline therefore materializes whatever the ORM defines at the moment it runs, rather
than a fixed schema. Measured on 2026-08-03: **215 of 271 tables** are created by no
explicit migration and arrive only that way.

A database created today receives all 271. A database that ran the baseline before a model
was added received the tables that existed then, and no later migration supplies the rest.
Both report head `0037_cross_stream_joins`.

Two consequences, the second easy to miss:

1. **Upgrade.** An existing pilot can silently lack tables that code, routes, and tests all
   assume. The interface tables are a live example — real, routed, and tested, but present
   only in databases created after those models landed.
2. **Evidence provenance.** `migration_head` is the staleness key in every Tier B evidence
   file and in `audit_evidence_corpus.py`. If head does not determine schema, two evidence
   files can both read CURRENT at the same head while describing different systems. The
   provenance model built on 2026-08-03 is weaker than it appears, and this defect is the
   reason.

P1 rather than P0: no committed data is lost, and a documented workaround exists — create
fresh and restore, per [`RECOVERY.md`](RECOVERY.md). It is not P2 because it degrades the
upgrade guarantee and quietly weakens the evidence rule the goal depends on.

**Fixed 2026-08-04** by `0038_explicit_schema_baseline`, which states all 271 tables and
849 indexes explicitly. Every statement is guarded, so it is a no-op on a database that
`0001` already populated and a catch-up on one that predates these models.

Verified against a real PostgreSQL rather than by inspection. A database stamped at `0037`
holding **zero tables** — the worst case an old deployment can present — upgraded to **272
tables** at `0038`, with the interface tables among them. A fresh install still reaches the
same 272, the upgrade is idempotent on a second run, and a full `downgrade base ->
upgrade head` round-trip passes on both SQLite and PostgreSQL. Baseline-only tables fell
from 215 to **0**, and `oms/test_schema_identity.py` now holds the ceiling there.

This does not retrofit determinism onto `0001`; it converges every deployment at `0038`
and the ratchet prevents new tables from reopening the gap. `downgrade` is deliberately
empty: this revision only adds tables a deployment was missing, and dropping them would
destroy data that `0001` created everywhere else.

Two consequences worth recording:

- Advancing the head **invalidated the one Tier B gate that was passing**. The chaos
  evidence was earned at `0037` and now reads STALE, so Tier B fell from 1 of 10 back to
  0 of 10. That is the non-completion rule working rather than failing, and the gate has
  to be re-run rather than re-dated.
- The fix exposed a silent flaw in the instrument built to police it. `current_head()`
  matched `revision = "..."` but Alembic autogenerate emits `revision: str = "..."`, so the
  auditor reported the old head and would have kept reporting evidence as CURRENT
  indefinitely. The regex now accepts both forms, and the migration follows the repository
  convention as well.

### GOAL2-006

`POST /interfaces/{id}/query-objects` walked every object type implementing an interface
and selected `ObjectInstance` rows by `object_type_id` alone. It took no principal, carried
no permission dependency, and applied no project filter. Two projects sharing one
interface, one call, and both projects' committed property values came back together.

An interface names a capability, not a tenancy scope, so walking its implementers crosses
projects by construction. Every other ontology read is scoped; this one was written as a
graph traversal and inherited none of that.

P0 under the severity gate, which names tenant isolation directly. Instance data crossed
the boundary, not metadata, and no configuration prevented it.

Fixed by resolving the readable project set from the principal before any instance is
touched: implementers are filtered to object types in readable projects, instances are
filtered again by their own `project_id` so a type readable in one project cannot surface
rows another wrote against it, and an explicit `project_id` in the request is authorized
rather than trusted. Omitting the scope now means "every project I may read" rather than
"every project".

`oms/test_interface_query_tenancy.py` proves the property that matters rather than the
mechanism: a principal confined to one project reaches nothing of the other's, neither by
asking for it nor by omitting the scope. Verified to fail with the fix reverted.

The defect predated this session but was latent while interfaces were unusable. Making
conformance check types turned an unreachable endpoint into a reachable one, so the
hardening created the exposure it then had to close.


### GOAL2-004, continued

This is deliberately left OPEN. Two defensible resolutions exist — widen the estimator
(more editors, or aggregate latencies across repeated rounds) or re-decide the threshold
against a stated percentile and sample size — and both change a declared Tier B gate.
The measurement contract requires such a change to be recorded with an owner and a
rationale rather than made silently by whoever is running the harness. Re-running until
a green number appears is explicitly not a resolution.

### GOAL2-007

The Tier B ontology-scale gate reads an object set with no residual filter. Every surface
a user touches sends a filter, a facet, or a spatial predicate, and each takes a different
path: `_query_object_rows` and `_logic_object_rows` both materialize the whole object type
before any limit applies.

Measured 2026-08-06 by `oms/measure_read_path_bounds.py` at 25k, 100k and 400k objects,
worst of three, on SQLite. Growth normalized to a 10x cardinality step:

| Shape | 400k latency | 400k peak heap | heap per 10x |
| --- | ---: | ---: | ---: |
| Typed read, no filter — what the gate measures | 1.3 ms | 0.1 MB | **x1.0** |
| Typed read, one equality filter | 2,235.1 ms | 302.5 MB | **x9.9** |
| Facet aggregation | 6,390.3 ms | 912.8 MB | **x10.0** |
| Spatial radius query | 7,119.4 ms | 912.8 MB | **x10.0** |

Heap is proportional to the object type, exactly and not approximately, on every shape
except the measured one. At the 10M objects the gate certifies, one filter click
extrapolates to ~7.6 GB and one map render to ~22.8 GB, per request, against a
collaboration gate that declares twenty concurrent editors.

Graded **P0**: it blocks the outcome path at the scale this goal declares, and the
Object Explorer and Operational Map are both on that path.

**Measured at reference scale 2026-08-06**, since the grade had rested on a linear
extrapolation from 4% of it. On PostgreSQL 15.8 at 10,000,000 objects, one filtered read
cost **21,940.6 MB and 157,654.5 ms**; the facet aggregation and the spatial query each
cost the same memory. The SQLite extrapolation had predicted ~7.5 GB, so it understated
the production dialect roughly threefold — Postgres had no equality pushdown at all, where
SQLite narrowed the candidate set first.

**Fixed 2026-08-06.** Both primitives stream and expunge as they go; the facet aggregation
accumulates counters rather than rows; the spatial query keeps a bounded heap instead of
sorting every match; and equality and existence predicates now push into SQL on both
dialects. On the same ten million rows, the filtered read is **8.2 ms and 1.6 MB**, and no
shape exceeds 6.8 MB.

The fix is memory-complete and latency-partial, which is stated rather than rounded off:
the facet aggregation improved 15% and the spatial query regressed 19%, because both still
visit every row — one owes an exact total, the other has no spatial index. Those are
conditions B3 and B4 of [`GOAL_2026-08-06.md`](GOAL_2026-08-06.md) and remain open.

Two aggravating details. The measurement ran on SQLite, whose `_pushdown_equalities`
pre-filter does not exist on any other dialect — production is Postgres, where the
filtered read materializes the whole type rather than the matching third, so these
numbers are the favorable case. And `_logic_object_rows` applies its `limit` to the list
returned by `.all()`, so the limit never reaches SQL and a call site asking for five rows
loads the type; all twenty-one of its call sites are unbounded whatever they pass.

### GOAL2-008

`audit_extensibility.py` reports `semantic types the UI can render 13 of 13`. All thirteen
matches come from `frontend/src/utils/semanticRender.tsx` — the renderer itself. One
workspace of twenty-two imports it. The audit would report 13 of 13 with no consumer at
all, because it measures the renderer's vocabulary and not its reach.

The visible consequence is on the Operational Map, whose inspector passes raw properties
to `KeyValueGrid` with no specs, so a polygon reaches the user as GeoJSON text on the one
surface where geometry is the entire point. `DataTable` has no specs parameter to pass.

Graded **P1**: the outcome path is degraded and the workaround is reading the raw value.

**Fixed 2026-08-06.** `DataTable` gained a specs parameter, and the Operational Map's
inspector and the decision timeline now pass the object type's declared properties, so
geometry, timestamps and units render as themselves on every surface that shows object
state. The audit gained a reach measure — call sites receiving specs, of call sites
rendering object properties — which reads **3 of 3** where the vocabulary count read 13 of
13 while one surface consumed it. The vocabulary count is retained but relabelled, because
it was never wrong, only misread.

The reach measure is deliberately conservative: it recognises `KeyValueGrid` and
`DataTable` call sites whose data expression names object properties, so a hand-rolled
table is not counted. It is a floor on the gap, not a census, and the audit prints its
scope so 3 of 3 is not read as the whole UI.

Both defects are the same species and neither standing invariant catches them, which is
why [`GOAL_2026-08-06.md`](GOAL_2026-08-06.md) adds a third.

### GOAL2-009

Introduced by the GOAL2-007 fix and found by its own test. Bounding memory meant not
letting a scan accumulate in the session's identity map, and the first approach was to
`expunge` each row as it was rejected. `expunge` does not distinguish a row this query
loaded from a row the caller loaded earlier and still holds: detaching the latter means
its subsequent modifications are never flushed. A handler that read objects spatially and
then edited one would have had that edit silently discarded.

Caught because a test wrote through an instance it had fetched before a spatial query and
the write did not land. Nothing raised.

Fixed by removing the reason to expunge: the streaming path selects columns rather than
entities, so a scan never enters the identity map and there is nothing to detach. All
twelve `expunge` calls are gone, and memory is lower than with them.

The general lesson is recorded against invariant 3 rather than here — a fix that is
correct in isolation can be wrong in composition, and the composition is where the product
lives.

### Tier B

Not claimed. Prior evidence exists for ontology scale, mixed workload, pipeline scale,
collaboration, identity, durability, and chaos, but was produced at earlier migration
heads and requires re-execution under the non-completion rule. Availability, RPO
sampling, and scheduled RTO rehearsal have no evidence file yet.

The three production Playwright profiles (`oidc-rbac`, `oidc-scale`, `plugin-executor`)
skipped in the Tier A run because their rehearsal credentials and environment were absent
on this host. They are Tier B evidence and are still owed.

### Tier C

Not claimed. No external evaluator run has been recorded.

## Evidence index

| Area | Artifact |
| --- | --- |
| Requirement conformance | [`foundry-docs/VALIDATION_MATRIX.md`](../foundry-docs/VALIDATION_MATRIX.md), `oms/validate_docs_conformance.py` |
| Production acceptance | [`GOAL_ACCEPTANCE_2026-07-28.md`](GOAL_ACCEPTANCE_2026-07-28.md), `scripts/rehearse-production-acceptance.ps1` |
| Delivered increments | [`GOAL_PROGRESS_2026-07-29.md`](GOAL_PROGRESS_2026-07-29.md) |
| Ontology scale | [`ONTOLOGY_SCALE_BENCHMARK.md`](ONTOLOGY_SCALE_BENCHMARK.md), `ontology-scale-reference-evidence.json` |
| Mixed workload | [`ONTOLOGY_MIXED_WORKLOAD_BENCHMARK.md`](ONTOLOGY_MIXED_WORKLOAD_BENCHMARK.md) |
| Pipeline scale | [`PIPELINE_SCALE_BENCHMARK.md`](PIPELINE_SCALE_BENCHMARK.md) |
| Collaboration | [`COLLABORATION_SCALE.md`](COLLABORATION_SCALE.md), `collaboration-websocket-chaos-evidence.json` |
| Identity | `oidc-identity-scale-evidence.json` |
| Recovery | [`RECOVERY.md`](RECOVERY.md), `ontology-scale-backup-restore-evidence.json`, `ontology-scale-replica-failover-evidence.json` |
| Plugin isolation | [`SIGNED_PLUGIN_RUNTIME.md`](SIGNED_PLUGIN_RUNTIME.md), `plugin-oci-rehearsal-evidence.json`, `plugin-egress-rehearsal-evidence.json` |
