# Docs-Grounded Validation Matrix

This matrix validates the local Ontology AIP Platform against public Foundry/AIP behavior and the local `foundry-docs` corpus. It is a behavioral conformance matrix, not a proprietary API or source-code comparison.

Status legend:

- `MATCH`: the local implementation supports the documented behavior directly.
- `LOCAL_ANALOG`: the local implementation provides the same architectural role with deterministic local substitutes.
- `PARTIAL`: the behavior exists but lacks documented depth or visual fidelity.
- `INTENTIONAL_DIFFERENCE`: the documented behavior depends on Palantir-managed infrastructure, private services, or proprietary UI internals and is represented locally by a bounded substitute.
- `MISSING`: no meaningful local behavior exists yet.

| Domain | Source | Documented behavior | Local evidence | Status | Gap / note | Priority |
|---|---|---|---|---|---|---|
| Ontology | https://www.palantir.com/docs/foundry/ontology/overview | Object types, properties, object instances, link types, and links form the semantic layer. | `/object-types`, `/objects`, `/link-types`, `/links`, `oms/test_docs_conformance.py` | MATCH | Local SQLite runtime, not Foundry ontology storage. | P0 |
| Ontology validation | `foundry-docs/ontology/ontology-overview.md` | Ontology resources should be validated for broken references and graph consistency. | `/ontology/validate`, `oms/test_ontology_validation.py`, `oms/test_docs_conformance.py` | MATCH | Validation is local and deterministic. | P0 |
| Object profiles | https://www.palantir.com/docs/foundry/object-views/overview | Users can inspect an object with properties, links, and contextual views. | `/objects/{type}/{id}/profile`, `oms/test_foundry_gis_features.py` | LOCAL_ANALOG | Not a full Foundry Object View renderer. | P1 |
| Action Types | https://www.palantir.com/docs/foundry/action-types/overview | Project-owned actions govern ontology writeback, validation, side effects, approvals, idempotency, and auditability. | Project-scoped `/action-types`, `/actions/execute`, `/approvals`, `/audit-logs/search`, `oms/test_governed_automation_tenancy.py`, production OIDC rehearsal | MATCH | Local transactional approval and outbox semantics; both canonical and legacy execution paths enforce project scope. | P0 |
| AIP Logic | https://www.palantir.com/docs/foundry/logic/overview | Project-owned visual block workflows chain variables, query objects, call LLM/tool blocks, and propose governed actions. | Project-scoped `/logic-functions`, `/logic-functions/{id}/run`, `oms/test_aip_logic.py`, `oms/test_governed_automation_tenancy.py`, production OIDC rehearsal | LOCAL_ANALOG | Deterministic local LLM stand-in with backend reference validation and execution permission enforcement. | P0 |
| AIP Evals | https://www.palantir.com/docs/foundry/aip-evals/overview | Project-owned evaluation suites validate Logic and agent quality while preserving governed run evidence. | Project-scoped `/eval-suites`, `/eval-runs`, `/aip/evals/*`, `oms/test_aip_evals.py`, `oms/test_ai_evaluation_tenancy.py`, production OIDC rehearsal | LOCAL_ANALOG | Deterministic local graders with project-bound suites/runs; no managed model evaluation plane. | P1 |
| Agent Studio | https://www.palantir.com/docs/foundry/chatbot-studio/overview | Project-owned agents retrieve governed ontology context, expose grounded tool traces, run recoverable tasks, and stage risky actions for approval. | Project-scoped `/agents`, `/agents/{id}/sessions`, `/aip/agents/{id}/invoke`, `/api/v1/agents/{id}/tasks`, `/task-graphs`, context/tool/synthesis workers, React task-stage evidence, `oms/test_aip_agent_async_execution.py`, `oms/test_agent_task_graph.py`, `oms/test_governed_automation_tenancy.py`, visible-control browser acceptance, production OIDC rehearsal | LOCAL_ANALOG | Deterministic local planner with immutable task configuration, independently retryable stages, dependency failure/cancellation, citations, and synthesis-only approvals; provider-neutral hosted inference remains adapter-driven. | P1 |
| Workshop | https://www.palantir.com/docs/foundry/workshop/overview | Project-owned apps compose widgets, variables, layouts, events, actions, preview, publish, and restore. | Project-scoped `/apps/workshop`, `/resolve`, `/render-live`, `/event`, `/publish`, `/versions`, `/workspace/workshop`, `oms/test_workshop.py`, `oms/test_workshop_tenancy.py`, production OIDC rehearsal | MATCH | UI builder is a local approximation with backend-enforced project isolation and approval staging for risky actions. | P0 |
| Object Explorer | https://www.palantir.com/docs/foundry/object-explorer/overview | Users search/filter objects, inspect histograms, drill into profiles, traverse links, save explorations, and run governed actions. | `/object-explorer/query`, `/histogram`, `/property-stats`, `/explorations`, `/objects/{type}/{id}/profile`, `/actions/execute`, typed React `/workspace/object-explorer`, `frontend/tests/evaluator.spec.ts`, `oms/test_docs_conformance.py` | MATCH | React exploration workbench is an original local analog, not Foundry UI internals. | P0 |
| Pipeline Builder | https://www.palantir.com/docs/foundry/pipeline-builder/overview | Project-owned visual DAGs validate, preview sampled output, deliver datasets/ontology outputs, and record lineage. | Project-scoped `/pipeline-builder/graphs`, `/validate`, `/preview`, `/deliver`, `/pipeline-runs`, `oms/test_pipeline_tenancy.py`, `oms/test_deep_foundry_programs.py`, production OIDC rehearsal | MATCH | Compiles to local Python transforms with backend-enforced project isolation, not Spark. | P0 |
| Pipeline Builder canvas | https://www.palantir.com/docs/foundry/pipeline-builder/overview | Users build pipelines by dragging nodes onto a board/canvas, preview nodes, inspect warnings, manage outputs, and understand build execution. | `/workspace/pipeline`, `/ui-state/pipeline`, `/ui-state/pipeline/{id}/canvas`, selected-node details, output rail summary, node insert/layout/preview/suggestions endpoints, typed DuckDB execution-policy controls, partition/finalizer evidence, React workbench modules, `oms/test_ui_state_pipeline_ontology.py`, visible-control `frontend/tests/evaluator.spec.ts` | LOCAL_ANALOG | Original React canvas supports builder and snapshot-native execution, including automatic fallback and human-readable shard state; it does not copy Foundry UI internals or managed compute. | P0 |
| Ontology output from Pipeline Builder | https://www.palantir.com/docs/foundry/pipeline-builder/overview | Pipeline outputs can write directly to Ontology object types. | `ontology_output` graph node, `/pipeline-builder/graphs/{id}/deliver`, `oms/test_ontology_generator_pipeline_canvas.py` | MATCH | Local object materialization, not Object Storage V2. | P0 |
| Ontology Generator | https://www.palantir.com/docs/foundry/object-link-types/create-object-type | Guided object type creation selects a backing datasource, maps properties, chooses primary/title keys, and can scaffold actions. | `/ontology-generator/drafts`, `/workspace/ontology`, generated profiles/actions/pipeline graph, `oms/test_ontology_generator_pipeline_canvas.py`, `oms/test_ui_state_pipeline_ontology.py` | LOCAL_ANALOG | Local deterministic generator and proposal evidence, not managed OMA workflow. | P0 |
| Ontology Manager workspace | `foundry-docs/ontology/ontology-manager.md` | Users inspect object type overview, properties, security, datasources, observability, capabilities, links, actions, usage, and history. | `/ui-state/ontology`, `/ui-state/ontology/object-types/{id}`, `/walkthrough`, `/sections/{section_id}`, metadata/index/open-from-pipeline endpoints, React Ontology Manager, `oms/test_ui_state_pipeline_ontology.py` | LOCAL_ANALOG | Local manager cards and walkthrough, not Foundry OMA internals. | P0 |
| Ontology release lifecycle | https://www.palantir.com/docs/foundry/ontologies/review-ontology-proposals | Ontology changes are isolated, validated, reviewed, approved, compared, and promoted before they affect the main environment. | `/ontology/change-sets`, `/ontology/revisions`, `/ontology/environments`, validation/review/publish/diff/rollback endpoints, React Releases workspace, `oms/test_ontology_release_lifecycle.py` | LOCAL_ANALOG | Immutable local revision snapshots and approval decisions replace Foundry Global Branching infrastructure. | P0 |
| Ontology health center | https://www.palantir.com/docs/foundry/observability/overview | Semantic resources expose health checks, quality findings, operational signals, and history so users can diagnose unsafe changes. | `/ontology/health/evaluate`, `/ontology/health/runs`, `/ontology/policies/simulate`, generated object views, React Health Center, `oms/test_ontology_health.py` | LOCAL_ANALOG | Deterministic graph, policy, object, and pipeline checks replace managed monitoring views. | P0 |
| Ontology schema registry and typed SDKs | https://www.palantir.com/docs/foundry/ontology-sdk/overview | Approved ontology metadata produces versioned, type-safe developer contracts and SDKs for application development. | `/ontology/registry`, `/compatibility`, `/publish`, `/schema`, `/sdk/{language}`, `/packages`, governed package downloads, `oms/ontology_cli.py`, React Schema Registry, `oms/test_ontology_schema_registry.py`, `oms/test_ontology_sdk_installation.py`, `oms/test_ontology_cli.py` | LOCAL_ANALOG | Immutable JSON Schema plus reproducible npm and Python wheel artifacts, checksums, audit evidence, and clean offline installation; not Foundry OSDK hosting or OAuth client management. | P0 |
| Pipeline-to-ontology write contract | https://www.palantir.com/docs/foundry/pipeline-builder/outputs-add-ontology-output | Ontology outputs map fields and primary keys, reject incompatible records, expose deployment validation, and preserve output evidence. | Structured `ontology_output` configuration, schema propagation, field lineage, preview reconciliation, upsert/insert/update modes, quarantine APIs, contract run history, `oms/test_pipeline_ontology_contracts.py` | LOCAL_ANALOG | Local transactional hydration and quarantine replace Foundry object storage and managed build services. | P0 |
| Datasets | https://www.palantir.com/docs/foundry/data-integration/datasets/index.html | Datasets are versioned, schema-bearing records consumed by pipelines and apps. | `/data-assets`, `/datasets/{id}/transactions`, `oms/test_data_integration.py` | LOCAL_ANALOG | Uses JSON records, not Foundry dataset files. | P1 |
| Data imports | Local extension | Users should bring CSV/JSON records into a project-owned, reviewable import job before promoting them to a dataset. | `/imports/csv`, `/imports/json`, `/imports/jobs`, `/promote-to-dataset`, Command Center import panel, `oms/test_import_tenancy.py`, `oms/test_productized_platform.py`, production OIDC rehearsal | LOCAL_ANALOG | Local parser and schema inference with backend-enforced project ownership; not Foundry connector infrastructure. | P0 |
| File imports and semantic mapping | Local extension | Users should upload CSV/JSON files, choose an operational template, validate field mappings, and generate project-owned ontology drafts. | `/imports/files`, `/imports/templates`, `/imports/jobs/{id}/validate`, `/imports/jobs/{id}/generate-ontology-draft`, Command Center mapping UI, `oms/test_import_tenancy.py`, `oms/test_docs_conformance.py` | LOCAL_ANALOG | Dependency-free local upload/mapping flow with authenticated audit attribution, not managed connector ingestion. | P0 |
| Import transforms | Local extension | Users should clean and enrich imported data before promotion, including type coercion, enum cleanup, unit normalization, geospatial derivation, MGRS conversion, and deduplication. | `/imports/jobs/{id}/mapping-suggestions`, `/imports/jobs/{id}/apply-transforms`, React Data Onboarding, `oms/test_productized_platform.py`, `oms/test_docs_conformance.py` | LOCAL_ANALOG | Deterministic local transform DSL, not Foundry-backed transforms. | P0 |
| Hybrid connector preview | Local extension | Users should preview connector samples and turn source records into reviewable import jobs without requiring external services. | `/connections/sources/{id}/preview`, `/connections/sources/{id}/generate-import-job`, `/connections/syncs/{id}/validate`, `oms/test_productized_platform.py`, `oms/test_docs_conformance.py` | LOCAL_ANALOG | Local sample-record preview with optional Docker/local service path later. | P1 |
| Stream replay | Local extension | Users should replay operational events deterministically, archive them to datasets, and use them as workflow evidence. | `/streams/{id}/replay`, stream archive evidence, `oms/test_productized_platform.py`, `oms/test_docs_conformance.py` | LOCAL_ANALOG | Local replay and archive, not hosted streaming infrastructure. | P1 |
| Data Health | https://www.palantir.com/docs/foundry/data-health/overview | Dataset checks monitor row counts, schema, freshness, and quality. | `/data-assets/{id}/expectations/run`, `/reliability/data-contracts`, `oms/test_ops_investigations_reliability.py` | MATCH | Local expectation engine. | P0 |
| Data Lineage | https://www.palantir.com/docs/foundry/data-lineage/overview | Downstream impact and pipeline provenance are visible. | `/lineage/graph`, `/reliability/lineage-impact`, pipeline run lineage | LOCAL_ANALOG | Local traversal only. | P1 |
| Map / GIS | https://www.palantir.com/docs/foundry/map/overview | Geospatial object layers, feature rendering, spatial search, accessible selection, risk styling, and map workspace are supported. | `/gis/spatial-query`, `/gis/feature-collection`, `/gis/map-layers`, `/gis/geofence/evaluate`, typed Leaflet React `/workspace/map`, four-viewport axe/browser checks in `frontend/tests/evaluator.spec.ts`, `oms/test_foundry_gis_features.py` | MATCH | OpenStreetMap provides external basemap tiles; ontology overlays, controls, evidence, and spatial evaluation are local. | P0 |
| MGRS | `foundry-docs/analytics/map-and-gis.md` | Operational GIS can use MGRS-style grid references. | `/gis/mgrs/encode`, `/gis/mgrs/decode`, `oms/test_docs_conformance.py` | MATCH | Local conversion implementation. | P0 |
| Model Integration | https://www.palantir.com/docs/foundry/model-integration/overview | Objectives, submissions, checks, releases, deployments, adapters, evaluation data, governed endpoint registration, and inference are managed in a lifecycle. | Project-scoped `/modeling/*`, `/model-endpoints`, `/aip/model-catalog/byom`, `oms/test_modeling_modelops_tenancy.py`, `oms/test_modeling_evaluation_ops.py`, migration and production rehearsal | LOCAL_ANALOG | Deterministic models with backend-enforced project ownership and portable lifecycle evidence; no managed training infrastructure. | P1 |
| ModelOps monitoring | `foundry-docs-full/modeling/model-deployments.md` | Project-owned objectives, submissions, evaluation gates, releases, deployments, inference, and monitors should form one governed operational lifecycle without leaking evidence across tenants. | Project-scoped `/modeling/*`, `/model-evaluations/*`, `/modelops/summary`, `/modelops/monitors`, `/run`, `/prediction-logs`, typed React `/workspace/models`, stateful lifecycle and four-viewport axe checks in `frontend/tests/evaluator.spec.ts`, `oms/test_modelops.py`, `oms/test_modeling_modelops_tenancy.py` | LOCAL_ANALOG | Local deterministic training, release gating, drift/quality profiles, and inference logs replace a managed model telemetry and compute plane. | P1 |
| Unified event bus | Local extension | Operational and ontology changes should be emitted atomically, searchable, resumable, project scoped, and routable into processing streams. | `/api/v1/outbox/*`, `/api/v1/events/log`, `/api/v1/event-stream-bindings/*`, `/events/*`, `event.dispatch` and `event.stream.route` workers, `oms/test_event_to_stream_routing.py`, `oms/test_docs_conformance.py` | LOCAL_ANALOG | The durable event log is the source of truth; local Ops events remain a searchable operational projection. | P1 |
| Global search and commands | Local extension | Users should search across objects, datasets, pipelines, actions, incidents, models, investigations, and navigation commands. | `/search/query`, `/search`, `/search/commands`, `/workspace/search`, `oms/test_unified_platform.py` | LOCAL_ANALOG | Local index is query-time over SQLite resources, not a managed search service. | P1 |
| Governance policy engine | Local extension | Access, masking, row filtering, approval requirements, break-glass, and policy simulation should be testable. | `/policies`, `/policies/evaluate`, `/policies/simulate`, `/policies/decisions`, `oms/test_unified_platform.py` | LOCAL_ANALOG | Local deterministic policy decisions; not Palantir identity enforcement. | P1 |
| Shared activity timeline | Local extension | Object, event, audit, snapshot, policy, and incident history should be inspectable from one timeline. | `/activity/timeline`, `/activity/objects/{type}/{id}/timeline`, `oms/test_unified_platform.py` | LOCAL_ANALOG | Timeline composes existing local audit/snapshot/ops records. | P1 |
| Platform graph overview | Local extension | Users should understand relationships among datasets, pipelines, ontology objects, links, and incidents. | `/graph/overview`, `/workspace/graph`, D3 visual graph canvas, filters, inspector, `oms/test_unified_platform.py`, `oms/test_productized_platform.py` | LOCAL_ANALOG | Visual canvas is local D3, not a managed graph product. | P1 |
| Asset Reliability Command Center | Local operational MVP | A user can go from raw maintenance data to pipeline hydration, ontology exploration, risk/model/data checks, agent recommendation, approval, governed action, incident, and report without code or raw JSON. | Permission-enforced `/scenarios/asset-reliability/*`, `/workflow-state`, `/workspace/command-center`, durable approval/action evidence, `oms/test_asset_reliability_command_center.py`, `oms/test_docs_conformance.py`, and the Playwright visible-control workflow | LOCAL_ANALOG | The responsive browser path bootstraps, triages, approves, executes once, and exports a report through human controls. This is the primary useful local workflow proof, not a Palantir product clone. | P0 |
| Validation dashboard | Local extension | Users should see implemented status, local analog status, test evidence, docs links, and intentional differences. | `/workspace/validation`, `/scenarios/asset-reliability/validation-dashboard`, `foundry-docs/VALIDATION_REPORT.md`, `oms/test_productized_platform.py`, `oms/test_docs_conformance.py` | LOCAL_ANALOG | Local docs-grounded conformance dashboard, not a Palantir certification. | P0 |
| Human evaluator shell | Local extension | A technical evaluator should see a clean guided workflow, readable states, proof trail, readiness checks, and no raw JSON in the primary path. | `/ui-state/command-center`, `/ui-state/imports`, `/ui-state/validation`, `/project/readiness`, `/project/demo/bootstrap`, React flow indicator and evaluator pages, `oms/test_human_ui_readiness.py` | LOCAL_ANALOG | Productized local evaluator UX, not a copied Foundry frontend. | P0 |
| React/Vite frontend | Local extension | The product shell should move from one large static script toward typed, componentized evaluator workspaces. | `frontend/package.json`, typed workspace clients, lazy Object Explorer, Leaflet GIS, ModelOps, Decision, and Ops chunks, `frontend/src/workspaces/*`, `frontend/src/components/*`, FastAPI React build serving, `npm run build`, stateful lifecycle and four-viewport axe/browser acceptance, `oms/test_productized_platform.py`, `oms/test_ui_state_pipeline_ontology.py` | LOCAL_ANALOG | All primary operational evaluator workspaces now use typed React; `?legacy=1` remains an explicit compatibility aid rather than the default route. | P1 |
| Schema and event health | Local extension | Runtime should expose predictable table availability and audit/event consistency checks. | `/system/schema-health`, `/system/event-consistency`, `oms/test_productized_platform.py`, `oms/test_docs_conformance.py` | LOCAL_ANALOG | Lightweight local checks, not enterprise observability infrastructure. | P1 |
| Migration metadata | Local extension | Runtime should expose local schema version and applied migration metadata for predictable startup. | `/system/migrations`, persisted `system_migration_records`, `oms/test_productized_platform.py`, `oms/test_docs_conformance.py` | LOCAL_ANALOG | Lightweight local migration ledger, not Alembic or managed tenant migrations. | P1 |
| Project export/import | Local extension | Local projects should be exportable and re-importable as a deterministic snapshot. | Project-scoped v3 `/project/export`, `/project/import/validate`, transactional `/project/import`, dependency-closure filtering, Control Panel Recovery, staged Postgres restore, `oms/test_project_snapshot_tenancy.py`, `oms/test_recovery_runtime.py`, `oms/test_productized_platform.py` | LOCAL_ANALOG | Portable transfer enforces export/restore permission, rejects foreign rows and references, excludes secrets, and requires credential rebinding; database backup remains authoritative. | P1 |
| Scenario report export | Local operational MVP | A user should export the command-center decision narrative with linked evidence IDs after the governed action. | Export-permission-enforced `/scenarios/asset-reliability/report`, action-linked audit evidence, Command Center download control, `oms/test_asset_reliability_command_center.py`, `oms/test_productized_platform.py`, `oms/test_docs_conformance.py`, Playwright download assertion | LOCAL_ANALOG | Markdown/JSON local export with durable completion evidence, not managed document publication. | P0 |
| Shared visual builder kernel | Public Pipeline Builder, Workshop, Ontology, and AIP Logic authoring concepts | Visual artifacts support typed catalogs, drag/drop, multi-select, copy/paste, undo/redo, autosave, validation targets, preview evidence, publish, restore, and retry-safe collaborative edits. | `/builder/catalogs/{artifact_type}`, `/artifacts/{id}/commands`, `/collaboration/commands`, durable request-hashed command receipts, `/preview`, React `VisualBuilder`, `oms/test_builder_kernel.py`, `oms/test_artifact_collaboration.py`, `oms/test_artifact_receipt_migration.py` | LOCAL_ANALOG | Local command/revision runtime with persistent cross-replica idempotency; not proprietary editor internals. | P0 |
| Collaborative artifact review | Public collaborative authoring and proposal concepts | Visual artifacts support revision-anchored comments, target-scoped reviewed proposals, independent production approval, safe rebasing, durable conflicts, and evidence-backed apply. | `/api/v1/artifacts/{id}/comments`, `/proposals`, React artifact review panel, `oms/test_artifact_review_workflows.py`, `oms/test_artifact_review_workflows_migration.py`, `oms/verify_artifact_review_postgres.py`, `docs/ARTIFACT_REVIEW_COLLABORATION.md` | LOCAL_ANALOG | Server-authoritative local review workflow; not proprietary collaboration internals. | P0 |
| Collaborative scale and stale-state fencing | Public collaborative visual authoring concepts | Concurrent replicas must allocate unique revisions, safely rebase disjoint stale-base edits, retain command evidence, and acknowledge a bounded editor load without lost updates. | Refreshed PostgreSQL row locks, atomic revision/receipt/event commits, authenticated cursor-resumable WebSocket, `oms/verify_collaboration_scale_postgres.py`, `oms/verify_collaboration_websocket_chaos_postgres.py`, `.github/workflows/ci.yml`, `docs/COLLABORATION_SCALE.md`, machine-readable collaboration/OIDC evidence | LOCAL_ANALOG | Two replicas and 20 editors pass a 250 ms command p95 gate; 200 distinct PKCE identities pass production acceptance. Replica termination/restart resumes three ordered WebSocket events with zero misses/duplicates at 209.067 ms maximum reconnect. Long-duration partitions remain open. | P0 |
| Durable execution control plane | Local production extension | Long-running transforms, models, reports, and AI workflows require durable queueing, worker ownership, progress, retry, cancellation, stale-worker recovery, and idempotent mutation commits. | `/jobs`, durable request-hashed job receipts, `/jobs/claim`, `/heartbeat`, `/complete`, `/fail`, `/jobs/summary`, `/events/stream`, Pipeline async preview/deliver, AIP Agent async invoke, `oms/test_async_job_runtime.py`, `oms/test_job_idempotency_migration.py`, `oms/test_pipeline_async_execution.py`, `oms/test_aip_agent_async_execution.py`, production cross-replica enqueue and abandoned-lease chaos acceptance | LOCAL_ANALOG | Local database-backed worker protocol; distributed executors remain pluggable. | P0 |
| Structured Pipeline transforms | https://www.palantir.com/docs/foundry/pipeline-builder/overview | Transform nodes should be configured through typed controls with schema, preview, warnings, and lineage evidence. | `/pipeline-builder/node-types`, `/graphs/{id}/nodes/{node_id}`, Pipeline inspector, `oms/test_pipeline_structured_config.py` | MATCH | Executes deterministic local records rather than managed Spark jobs. | P0 |
| Ontology datasource mapping | https://www.palantir.com/docs/foundry/ontology/overview | Dataset fields map to typed ontology properties with required-field checks and object hydration preview. | `/ontology/mappings/preview`, `/datasource-mappings`, Ontology drag/drop mapping panel, `oms/test_ontology_mapping_impact.py` | LOCAL_ANALOG | Mapping metadata is stored locally on the object type. | P0 |
| Ontology change impact | Public ontology dependency and safe-change concepts | Destructive changes should identify affected objects, links, pipelines, applications, and artifacts before publication. | `/ontology/changes/impact`, archive confirmation UI, `oms/test_ontology_mapping_impact.py` | LOCAL_ANALOG | Local dependency traversal, not managed Foundry impact infrastructure. | P0 |
| Governed ontology packages | Public ontology modeling, marketplace, and environment promotion concepts | Ontology schemas should be reusable as validated, immutable versions with dependencies, integrity evidence, namespaced installation, upgrade ownership, and guarded rollback. | `/ontology-packages/*`, `/ontology-package-installations/*`, Ontology Manager Governed Packages panel, `oms/test_tenancy_ontology_packages.py` | LOCAL_ANALOG | Local portable semantic manifests and SHA-256 integrity; not Palantir marketplace packaging internals. | P0 |
| Signed sandboxed extension runtime | Public connector, transform, widget, ontology package, and model integration extensibility concepts | Third-party extensions require immutable signed manifests, project and organization governance, capability declaration, destination-governed durable isolated execution, revocation, evidence, and recoverable bundles. | `/api/v1/plugins/*`, `plugin.execute` jobs, dedicated `plugin-execution` production/rehearsal profiles, `plugin-sdk/python`, `oms/plugin-sandbox.Dockerfile`, `oms/plugin-executor.Dockerfile`, `oms/plugin-egress-proxy.Dockerfile`, `frontend/tests/production/plugin-executor.spec.ts`, `oms/test_plugin_executor_production_rehearsal.py`, `oms/test_plugin_egress_policy.py`, `oms/test_plugin_egress_rehearsal.py`, `docs/plugin-executor-production-evidence.json`, `docs/plugin-egress-rehearsal-evidence.json`, `docs/SIGNED_PLUGIN_RUNTIME.md` | LOCAL_ANALOG | Ed25519 registry, typed SDK contracts, portable recovery, execute-only OIDC bootstrap, lease-fenced pull execution, executor-loss recovery, duplicate-terminal prevention, exact signed egress destinations, signed custom-CA HTTPS, short-lived HMAC grants, private-address denial, direct-bypass denial, production proxy-readiness evidence, and digest-pinned OCI evidence are implemented without exposing an OCI socket to the API. | P0 |
| Organization and project isolation | Public Foundry security and project permission concepts | Global roles and project membership jointly control artifacts, ingestion, ontology, pipelines, Workshop, actions, AIP Logic, agents, complete model/ModelOps lifecycles, evaluations, packages, operational resources, and backup/restore. | `/tenancy/*`, project-scoped product APIs, OIDC tenant claims, `oms/test_governed_automation_tenancy.py`, `oms/test_ai_evaluation_tenancy.py`, `oms/test_modeling_modelops_tenancy.py`, `oms/test_operational_plane_tenancy.py`, `oms/test_project_snapshot_tenancy.py`, production OIDC rehearsal | LOCAL_ANALOG | Persisted authorization boundary with project-bound jobs, approvals, modeling and operational evidence, cross-resource validation, migrations, and dependency-complete snapshot recovery. | P0 |
| Durable connector and streaming ingestion | Public Foundry data connection and streaming concepts | Connector syncs and stream replays should execute asynchronously with project isolation, leases, retries, idempotency, budgets, run telemetry, dead-letter recovery, and portable evidence. | `/connectors/adapters`, `/connections/sources/{id}/runtime-credentials`, `/live-preview`, `/fetch-attempts`, `/ingestion/*`, `/streams/*`, `oms/test_live_connector_runtime.py`, `oms/test_s3_connector_runtime.py`, `oms/test_sftp_connector_runtime.py`, `oms/test_kafka_connector_runtime.py`, `oms/test_durable_ingestion_runtime.py`, `oms/rehearse_kafka_connector.py`, `docs/DURABLE_INGESTION_RUNTIME.md` | LOCAL_ANALOG | REST, PostgreSQL, S3-compatible storage, pinned-host SFTP, and Kafka execute live with encrypted credentials, transport safeguards, bounded decoding, and durable file/object/partition cursors. | P0 |
| Production deployment acceptance | Local production extension | A release should prove real OIDC tenant claims, backend RBAC, project-owned onboarding, builders, governed automation, concurrent access, recovery, and staged database restore. | `.github/workflows/ci.yml`, `docker-compose.rehearsal.yml`, `frontend/tests/production/oidc-rbac.spec.ts`, `frontend/tests/production/oidc-scale.spec.ts`, `frontend/tests/production/pipeline-worker-recovery.spec.ts`, `scripts/rehearse-production-acceptance.ps1`, `scripts/rehearse-recovery.ps1`, `docs/oidc-identity-scale-evidence.json`, `docs/pipeline-worker-container-recovery-evidence.json`, `docs/pipeline-worker-multidaemon-recovery-evidence.json`, project-scope and collaboration tests | LOCAL_ANALOG | Clean rehearsal verifies restart recovery, 200 distinct PKCE identities across two replicas, 50-reader load, collaboration chaos, OIDC-authenticated worker loss across independent Docker daemons and private caches, migration serialization, and fresh-volume restore. Sustained pilot availability and true host loss remain separate gates. | P0 |
| Runtime observability and cost governance | Public Foundry observability and resource-management concepts | Durable workflows require correlated traces, progress, latency, usage and cost evidence, project budget admission, SLO evaluation, breach events, and tenant-safe operational views. | `/runtime/observability/*`, `/runtime/pilot-evidence`, `/health/pilot-recovery/*`, `/ui-state/runtime-operations`, Control Panel Runtime tab, production `pilot-observability` service, `oms/pilot_postgres_recovery.py`, `docker-compose.pilot-recovery.yml`, `oms/test_runtime_observability.py`, `oms/test_pilot_evidence_observer.py`, `oms/test_pilot_recovery_protocol.py`, `oms/test_pilot_window_scheduler.py`, `oms/test_pilot_postgres_recovery_driver.py`, `docs/RUNTIME_OBSERVABILITY.md` | LOCAL_ANALOG | Deterministic local cost estimates and database-backed telemetry replace managed provider billing and telemetry services. A live two-project reference rehearsal authenticated the backup manifest, restored a current-head durability mark plus local snapshot/plugin volumes into fresh isolated storage, and accepted an authenticated write; the seven-day availability and repeated isolated recovery windows still require real pilot wall clock. | P0 |
| Distributed worker fleet and fair queues | Public Foundry build, scheduling, and compute-management concepts | Multiple workers must claim each job once, enforce capability and project boundaries, respect worker/project concurrency, drain safely, execute compute outside the API, recover expired leases, and prevent stale commits. | `python -m app.worker_daemon`, production `workers` profile, hashed service tokens, `/runtime/workers/*`, `/runtime/queues/*`, `/jobs/claim`, `/ui-state/worker-fleet`, `oms/test_worker_daemon.py`, `oms/test_worker_fleet_control.py`, `oms/test_pipeline_worker_deployment.py`, `oms/rehearse_pipeline_worker_recovery.py`, `frontend/tests/production/pipeline-worker-recovery.spec.ts`, `docs/pipeline-worker-recovery-evidence.json`, `docs/pipeline-worker-container-recovery-evidence.json`, `docs/pipeline-worker-multidaemon-recovery-evidence.json`, `docs/WORKER_DAEMON.md` | LOCAL_ANALOG | Independently deployable Python workers own DuckDB compute and private S3 caches while PostgreSQL owns leases and fenced publication. Process, production-container, and independent-Docker-daemon rehearsals recover attempt two into one output; managed multi-host orchestration remains outside this local analog. | P0 |
| Normalized semantic and temporal ontology | Public ontology object, property, link, action, and history concepts | Ontology contracts should be typed, version-bound, and reusable while operational objects retain auditable point-in-time history. Published consumers must identify the exact ontology revision and properties they use so impact analysis is precise. | `/api/v1/ontology/compile`, `/api/v1/ontology/schema/definitions`, `/api/v1/ontology/contracts/validate`, `/api/v1/ontology/contracts/bind`, `/api/v1/ontology/contracts/bindings`, `/api/v1/ontology/contracts/health`, `/api/v1/objects/query`, `/api/v1/objects/*/history`, recoverable materialization generations, Workshop/artifact publish and pipeline deliver hooks, Ontology Manager contract/release UI, `oms/test_ontologyos_runtime_core.py`, `oms/test_ontology_contract_bindings.py`, `oms/test_industrial_materialization_lifecycle.py`, `frontend/tests/evaluator.spec.ts` | LOCAL_ANALOG | Current state stays in PostgreSQL JSONB while normalized definitions and append-only events avoid an EAV object-value model. Published Pipeline, Workshop, and visual-artifact consumers persist immutable version/revision/checksum/property bindings; strict production publication rejects drift, property-aware impact traverses exact consumers, and publish/rollback expose current, compatible-stale, or broken health. The human release flow lists affected versions, while the backend requires the exact current binding-ID acknowledgement set before breaking publication to prevent impact races. Source-backed objects missing from a later materialization are retired, excluded from current queries, preserved for audit, and reactivated if they return. | P0 |
| Scalable typed object and graph queries | Public object exploration, ontology indexing, and linked-object traversal concepts | Typed property queries and bounded graph expansion should use governed physical indexes, avoid row-wise metadata lookups, expose stable plans, and enforce measured latency gates during reads, bounded mutation, backup, and failover. | `BTREE_EXPRESSION_V3`, `/api/v1/objects/query`, `/api/v1/graph/query`, strict scale/mixed benchmarks, physical backup, restart, and streaming-replica rehearsals, benchmark/recovery contract tests, `docs/ONTOLOGY_SCALE_BENCHMARK.md`, `docs/ONTOLOGY_MIXED_WORKLOAD_BENCHMARK.md`, machine-readable evidence, PostgreSQL CI smoke profiles | LOCAL_ANALOG | Strict 10M-object/50M-link evidence measures exact lookup 8.718 ms p95, range/order 11.830 ms p95, and bounded two-hop expansion 13.721 ms p95. Under 100,000 temporal writes and eight readers, typed reads measured 53.635 ms p95 and throughput 213.932 writes/s. A 36.59 GB fresh-volume physical backup completed in 50.917 s and restored readiness in 1.144 s; streaming replay measured 0.015 s and promotion after source loss 0.686 s with the committed probe preserved. Multi-primary and universal capacity remain unclaimed. | P0 |
| Snapshot data plane and compiled pipeline plans | Public dataset snapshot, Pipeline Builder, lineage, and execution concepts | Bulk records and executable plans should be immutable, versioned, recoverable, and decoupled from UI metadata. | `/api/v1/datasets/*/snapshots`, `/api/v1/pipelines/*/plans`, `/api/v1/industrial/workflows/asset-reliability/onboard`, local/S3 storage adapters, `oms/test_ontologyos_runtime_core.py`, `oms/test_industrial_snapshot_workflow.py`, `oms/test_industrial_async_recovery.py`, `docs/ONTOLOGYOS_RUNTIME_CORE.md` | LOCAL_ANALOG | Local Parquet/JSONL and durable jobs replace managed object storage and distributed compute. Project-owned Connect-to-Report accepts registered Parquet with no embedded rows, compiles a DuckDB plan, writes an immutable output snapshot, and reconciles ontology objects in durable 1,000-row checkpoints. Leased worker retries resume both ontology hydration and full-scope decision partitions from committed checkpoints; idempotent replay reuses one execution/output, and duplicate terminal delivery is ignored. | P0 |
| Compatibility-preserving API v1 | Local production extension | Existing product APIs should remain usable while a typed, versioned contract is introduced without duplicating behavior or weakening authorization. | `/api/v1/compatibility/manifest`, `oms/app/api_v1_compat.py`, `oms/test_api_v1_compatibility.py`, `oms/test_api_v1_route_coverage.py`, `oms/audit_route_coverage.py`, `docs/ROUTE_COVERAGE.md` | LOCAL_ANALOG | Assembly-time aliases retain the exact FastAPI endpoint, validation, response, dependency, and permission contract. Explicit v1 handlers win collisions; deployment surfaces remain intentionally unversioned. | P0 |
| Snapshot-native pipeline scale and recovery | Public dataset snapshot, Pipeline Builder, lineage, spatial transform, quality, and reliable-output concepts | Large immutable partitioned inputs should register from bounded local or S3 connector outputs, execute without Python row hydration, compose parameters safely across branches, expose bounded previews, preserve source manifests in lineage, write recoverable partitioned outputs, govern cache capacity, and recover retries without duplicate snapshots. | `/api/v1/datasets/{asset_id}/snapshots/register`, bounded S3 prefix discovery, immutable per-file manifests, dependency-gated `pipeline.duckdb.partition` and `pipeline.duckdb.finalize` jobs, integrity-checked fragments, lease-fenced publication, branching DuckDB plans, atomic Hive-partitioned delivery, durable pipeline jobs, `oms/test_distributed_duckdb_pipeline.py`, `oms/test_partitioned_snapshot_pipeline.py`, `oms/test_s3_snapshot_pipeline.py`, `oms/rehearse_s3_snapshot_minio.py`, `oms/benchmark_object_storage_minio.py`, `oms/rehearse_pipeline_worker_recovery.py`, `frontend/tests/production/pipeline-worker-recovery.spec.ts`, `oms/benchmark_pipeline_scale.py`, `docs/WORKER_DAEMON.md`, scale and recovery contract tests, machine-readable evidence, CI smoke profiles | LOCAL_ANALOG | Row-local plans over one immutable multi-file input now split into exact manifest shards, materialize independently hashed Parquet fragments, release one blocked finalizer only after all shards succeed, and publish one idempotent snapshot with child-job lineage. Unsafe global operations are rejected or explicitly fall back in auto mode. Strict 10M-row and complex-plan profiles plus independent-Docker-daemon recovery remain green. Intended-provider networks and true physical multi-host caches remain open. | P0 |
| Provider-neutral governed model gateway | Public model integration and governed AIP execution concepts | Model providers should be replaceable while policy, evidence, idempotency, and action approval remain platform-owned. | `/models/gateway/*`, `/api/v1/models/gateway/*`, `/api/v1/agents/*/tasks`, `oms/test_ontologyos_runtime_core.py` | LOCAL_ANALOG | Deterministic local inference is default; explicitly enabled OpenAI-compatible providers are optional. | P0 |
| Transactional operational event delivery | Public Foundry event, audit, streaming, and operational reliability concepts | Domain mutations must commit delivery intent atomically, dispatch with recoverable leases, avoid duplicate publication, expose resumable consumption, and retain dead-letter evidence. | `/api/v1/outbox/*`, `/api/v1/events/log`, `/api/v1/events/stream`, `/api/v1/event-stream-bindings/*`, `ontologyos.object_change`, `event.dispatch`, `event.kafka.dispatch`, and `event.stream.route` workers, `oms/test_transactional_event_outbox.py`, `oms/test_object_change_event_outbox.py`, `oms/test_event_to_stream_routing.py`, `oms/verify_event_stream_routing_postgres.py`, `docs/TRANSACTIONAL_EVENT_OUTBOX.md` | LOCAL_ANALOG | Audit, Ops, and ontology object-change intents commit atomically with their evidence. Project bindings route stable event envelopes into ordinary streams with exact receipts, recoverable cursors, and concurrent-router fencing; Kafka/Redpanda remains an optional independent transport. | P0 |
| Durable event-time stream processing | Public streaming, incremental pipeline, data-health, and recovery concepts | Stream execution should preserve deterministic arrival, partition-local event-time progress, late-data evidence, bounded backlogs, recoverable state, idempotent output, governed event-log inputs, two-stream temporal correlation, outer semantics, and bounded join state. | `/api/v1/streams/processors/*`, `/api/v1/event-stream-bindings/*`, `stream.process` and `event.stream.route` workers, immutable pair/outer receipts, `oms/test_durable_stream_processing.py`, `oms/test_event_to_stream_routing.py`, `oms/test_cross_stream_interval_join.py`, `oms/test_cross_stream_outer_join.py`, `oms/test_stream_join_compaction.py`, `oms/test_cross_stream_join_tenancy.py`, `oms/verify_stream_processing_postgres.py`, `oms/verify_cross_stream_join_postgres.py`, `oms/verify_cross_stream_partition_postgres.py`, `oms/verify_cross_stream_outer_partition_postgres.py`, `docs/DURABLE_STREAM_PROCESSING.md` | LOCAL_ANALOG | Local transactional processors provide governed event routing, per-side watermarks, windows, quarantine, backpressure, replay-safe inner/left/right/full interval joins, explicit idle-key watermark advancement, immutable unmatched receipts, watermark-safe bounded state compaction, and exact rollback/replay evidence for interrupted inner and outer transactions. Distributed broker partition assignment and sustained throughput evidence remain future work. | P0 |
| Decision Intelligence | Local extension | Objects become project-isolated, explainable, temporal, scored, scenario-aware, resolvable, and agent-actionable. | `/decision/*`, `/temporal/*`, `/entity-resolution/*`, typed React `/workspace/decision`, `oms/test_decision_intelligence.py`, `oms/test_decision_intelligence_tenancy.py`, `oms/test_decision_intelligence_project_scope_migration.py`, project snapshot recovery | LOCAL_ANALOG | Original local decision workspace with backend-enforced project boundaries, append-only evidence, and recoverable decision resources. | P0 |
| Project-owned industrial Connect-to-Report | Local operational MVP | A promoted organization dataset should compile into a normalized, immutable, registered ontology contract, executable hydration pipeline, spatial fields, materialized objects, explainable findings, governed actions, and evidence-backed reports without code. | `/api/v1/industrial/workflows/asset-reliability/onboard`, `/triage`, `/workflow-state`, `/report`, `/evaluator-evidence`, `/ontology/schema/definitions`, `/ontology/revisions/*`, `/ontology/registry/*`, generic `/approvals/*` and `/actions/execute`, Command Center structured mapping, governance, and sealed evidence controls, `namespace_id` pipeline transform, `oms/test_industrial_asset_workflow.py`, `oms/test_external_evaluator_evidence.py`, `oms/test_industrial_async_recovery.py`, `oms/test_industrial_reconciliation_recovery.py`, `frontend/tests/evaluator.spec.ts` | LOCAL_ANALOG | Project-isolated workflow covers compatibility validation, immutable production revision, normalized properties/resources, typed registry contract, revision-bound object events, pipeline, MGRS/geometry, full-scope active-only risk counts, agent citations, approval, temporal mutation, outbox, incident, investigation, audit/Ops evidence, and report export. Leased background onboarding has independent hydration, retirement-reconciliation, and decision checkpoints. Missing source objects retire without losing state, then reactivate when restored. The external evaluator export binds own-data provenance, OIDC principal, release, migration, all workflow stages, report, and canonical hash; strict corpus validation requires two independent teams and does not claim them before submissions exist. A 10,501-object contract retains all band counts while bounding detailed findings to 100; it is not managed Foundry onboarding. | P0 |
| Operational Control Plane UI | Local extension | Operational events should be promoted through deterministic alert rules, incidents, governed runbooks, reliability evidence, and an acknowledgeable team inbox. | `/ops/*`, `/reliability/*`, typed React `/workspace/ops`, stateful event-to-inbox workflow and four-viewport axe checks in `frontend/tests/evaluator.spec.ts`, `oms/test_ops_investigations_reliability.py` | LOCAL_ANALOG | Original local operations surface connected to platform event, approval, incident, and reliability records. | P2 |
| Ops Control Plane | Local extension | Operational events, alerts, incidents, runbooks, approvals, and inbox are unified. | `/ops/*`, `/workspace/ops`, `oms/test_ops_investigations_reliability.py` | LOCAL_ANALOG | Local operating model, not Palantir-managed alerting. | P2 |
| Investigations | Local extension | Cases combine objects, evidence, graph, timeline, hypotheses, and reports. | `/investigations/*`, `/workspace/investigations`, `oms/test_ops_investigations_reliability.py` | LOCAL_ANALOG | Inspired by lawful investigation workflows, not a Gotham clone. | P2 |
| Security and governance | https://www.palantir.com/docs/foundry/security/overview | Permissions, markings, restricted views, audit, and governance checks shape access. | `/projects`, `/roles`, `/access/check`, `/markings`, `/restricted-views`, `/governance/scan`, tests under `oms/test_security_*.py` | LOCAL_ANALOG | Does not implement Palantir identity or enforcement services. | P1 |
| Private tenant pages | User-provided tenant URLs or screenshots | Tenant-specific applications and screenshots can inform visual/workflow validation. | User-provided evidence only | INTENTIONAL_DIFFERENCE | Do not scrape or redistribute private tenant content. | P2 |
| Managed infrastructure | Public docs across Foundry | Foundry uses hosted services for Spark, auth, storage, LLM routing, telemetry, deployment, and collaboration. | Local FastAPI, SQLite/Postgres, deterministic functions | INTENTIONAL_DIFFERENCE | This project intentionally stays local and deterministic. | P0 |

Required conformance target for this phase: all P0 rows must be `MATCH`, `LOCAL_ANALOG`, or `INTENTIONAL_DIFFERENCE`; no P0 row may be `MISSING`.
